# 2020 转写审计说明

## 范围

- 已创建 `word/past-exams/reviewed/2020.tex`，包含 2020-2021-2 学期《工科数学分析（二）》A 卷与 B 卷两节。
- 每个 `\section` 下均包含 `\subsection{试题}` 与 `\subsection{答案与解析}`。
- 题型标题均使用 `\paragraph*{...}`，未使用编号 paragraph。
- 未修改 `elegantbook2/elegantbook2.tex`，未删除文件。

## 页覆盖

- A 卷来源：`elegantbook2/试卷/2020级工数下A无答案.pdf`。
- A 卷页面图片：`word/past-exams/pages/2020级工数下A无答案/page-1.png` 至 `page-5.png`，共 5 页，已逐页覆盖。
- A 卷 raw 文本：`word/past-exams/raw/2020级工数下A无答案.txt` 基本为空，本稿主要依据页面图片目视转写。
- B 卷来源：`elegantbook2/试卷/2020级B卷无答案.pdf`。
- B 卷页面图片：`word/past-exams/pages/2020级B卷无答案/page-1.png` 至 `page-6.png`，共 6 页，已逐页覆盖。
- B 卷 raw 文本：`word/past-exams/raw/2020级B卷无答案.txt` 可用；本稿以 raw 为骨架，并用页面图片校正公式上下限、方向与页末题面。

## 已校正的 OCR/排版点

- B 卷一、3 图片确认内层积分为 `\int_{y^2}^{y} y\sin(x^2)\,dx`，raw 文本中上下限排版易误读。
- B 卷一、5 图片确认分母为 `\sqrt{x^2+y^2+z^2}`，不是 raw 文本中换行造成的其他结构。
- A 卷一、3 图片确认积分为 `\int_0^2 dx\int_{x/2}^1 e^{y^2}\,dy`。
- A 卷二、5 图片确认幂级数分子按 `2n^2+1` 转写。

## 不确定点

- A 卷一、2 的分段函数闭开区间按图片转写为 `|x|\le \pi/2` 与 `\pi/2<|x|\le\pi`；因该题只求 Fourier 和函数，间断点处已按左右极限平均值处理。
- A 卷二、5 的分子 `2n^2+1` 与分母 `(2n+1)!` 图片可辨但扫描略糊，建议后续人工复核原 PDF。
- B 卷三、1 页面确认原卷把第二个求和下限也印作 `n=0`，但待证级数含分母 `n`；本稿正文标为原卷下限问题，并按第二个级数从 `n=1` 处理。
- 两份 PDF 均为无答案卷，`答案与解析` 为本次补写；建议后续抽样核对复杂计算题符号方向，尤其第二类曲面积分的“下侧”方向。
