# 批次 001 GPT-5.5 只读复核包

## 使用目的

该文件用于后续稳定调用 GPT-5.5 子代理时作为最小上下文，避免再次让子代理扫描整仓或读取大量无关文件。

## 只读复核范围

- 主文件：`elegantbook2.tex`
- 全局 `solution` 序号：`1-10`
- 当前源码行号：`1273-1726`
- 对应小节：`第一节习题：模法与多项式逆元`
- 题型：有理分式分解、模法、带余除法、裴蜀恒等式、重根求导、混合重因式升幂

## 子代理复核指令

请作为 GPT-5.5 只读复核子代理，只审阅 `elegantbook2.tex` 当前行号 `1273-1726` 的十个 `solution`，不得修改任何文件。

复核标准如下：

1. 排版：是否仍有过度压行、超长行内公式、公式链未拆分、代码缩进混乱。
2. 逻辑：是否仍存在关键推导跳步；“可得、于是”等词是否已经有足够中间步骤支撑。
3. 方法：主解法是否符合前文“模法：有理分式与多项式逆元”口径，尤其是带余除法、裴蜀恒等式、掩盖赋值、重根求导、重因式每层写全。
4. 数学：最终分解是否存在符号、系数、漏层、分子次数不合规等风险。
5. 放行：若无残留缺陷，请明确输出“批次 001 可放行”；若存在任何建议，请逐题列出退回项。

请输出结构化中文报告，格式为：

```text
批次 001 GPT-5.5 复核结论：
- 总体结论：通过 / 退回
- 逐题意见：
  1. ...
- 残留缺陷：
  - 无 / ...
- 放行意见：
  - 可放行 / 不可放行
```

## 主端已完成的核验

- 全书 `solution` 数量：1322。
- `solution_quality_audit/solution_index.csv` 覆盖：1322 行。
- 首批当前索引：全局 `1-10`，行号 `1273-1726`。
- 主端人工/符号核验：十个最终分解式均与原式等价。
- 主端排版核验：首批范围内未发现超过既定阈值的超长公式行。

## 当前批次门槛

在 GPT-5.5 子代理返回“无异议/可放行”之前，主流程不得进入全局 `11` 号及以后 `solution` 的批量整改。
