# 批次 031 整改复审记录

生成时间：2026-06-23

## 0. 执行边界

- 批次范围：全局 `#747-752`
- 源码范围：`elegantbook2.tex:32932-33121`
- PDF 复核页：物理页 `520-523`，对应印刷页约 `470-473`
- 本轮主题：圆锥通量、旋度定义、Stokes 公式与空间曲线积分、旋度线性性质。
- 本轮复核口径：同步检查公式排版、推导完整性、方法与章节知识点一致性、方向/法向判定、计算结论正误。公式拆分只针对主计算或确实偏长的行内公式；短小定义、变量说明和小结论不机械改成行间公式。

## 1. 已完成的主要整改

- `#747`：将圆锥曲面通量的 Gauss 总通量计算改为行间主公式，保留底面通量为零的说明，结论为 `\pi`。
- `#748`：将旋度定义和 Stokes 公式作为核心定义公式行间展示，条件叙述保持文字化，不额外扩张短公式。
- `#749`：将 Stokes 计算中的长链条拆成分步公式，分别写清旋度、法向、面积或投影面积，再给出线积分结果。
- `#750`：将旋度直接计算和 `rot(grad u)=0` 的混合偏导验证改成更清晰的行间推导。
- `#751`：补强空间 Stokes 两题的解题思路，明确为什么可取同边界平面圆盘，并写清方向对应的法向选择。
- `#752`：旋度线性性质证明原本结构完整，仅复核其分量展开、合并和结论，未做不必要改写。

## 2. 逐题内容复核摘要

| 全局号 | 复核结论 |
| ---: | --- |
| 747 | 用 Gauss 公式处理由曲面 `z=1-\sqrt{x^2+y^2}` 与底面 `z=0` 围成的圆锥体，`\operatorname{div}\vec r=3`，体积为 `\pi/3`，总通量为 `\pi`；底面上 `\vec r\cdot\vec n=0`，故曲面通量 `\pi` 正确。 |
| 748 | 方向旋量、旋度定义、Stokes 成立条件与本节知识点一致；旋度分量顺序为 `(R_y-Q_z,\ P_z-R_x,\ Q_x-P_y)`，Stokes 公式的边界方向与曲面法向由右手规则配合。 |
| 749 | 第一题 `\vec F=(z,x,y)`，旋度 `(1,1,1)`，上侧单位法向 `(1,1,1)/\sqrt3`，三角形面积 `\sqrt3/2`，结果 `3/2` 正确。第二题旋度为 `(-2,-2,-2)`，按从 `x` 轴正向看逆时针取法向 `(b,0,a)/\sqrt{a^2+b^2}`，椭圆盘面积 `\pi a\sqrt{a^2+b^2}`，结果 `-2\pi a(a+b)` 正确。 |
| 750 | 第一问按旋度公式得 `(2y,-2x,\mathrm e^x\sin y-2x)`；第二问用二阶连续偏导下混合偏导可交换，得 `\nabla\times(\nabla u)=\vec0`，推导与前文旋度定义一致。 |
| 751 | 第一题取平面 `z=3y+1` 内圆盘，向量面积元 `(0,-3,1)\,\mathrm dx\,\mathrm dy` 与从 `z` 轴正向看逆时针匹配，被积函数化为常数 `2`，结果 `8\pi` 正确。第二题取平面 `x+z=R` 内圆盘，题给方向对应法向 `(1,0,1)/\sqrt2`，交圆半径 `R/\sqrt2`，结果 `-\frac{\sqrt2}{2}\pi R^2` 正确。 |
| 752 | 设分量后按旋度定义展开，利用偏导线性把 `\vec F`、`\vec G` 两部分分离，得到 `\operatorname{rot}(\vec F+\vec G)=\operatorname{rot}\vec F+\operatorname{rot}\vec G`，证明完整。 |

本轮未发现计算错误；主要问题是原解里若干主推导公式过长且分步不够清楚，已同步补强。短 display 中保留的是旋度定义、Stokes 主公式、面积元、主积分等解题骨架，不属于应压回行内的变量小定义。

## 3. 公式排版复核

- 重建索引：`python solution_quality_audit/build_solution_index.py`
  - `total_solutions = 1322`
  - `long_inline_flagged = 197`
  - `short_display_flagged = 761`
- 本批 `#747-752` 的 `inline_long_count` 合计为 `0`。
- 本批保留的行间公式均为定义公式、Stokes 转换、旋度展开、曲面积分面积元或最终主计算链条；没有把短变量定义、小代入、小结论随意拆成行间公式。
- 按用户给定的约束，行内公式只有接近约 `3/4` 行宽才作为优先拆分对象；本批剩余行内公式视觉上未接近该阈值。

## 4. PDF 版面复核

已渲染并目检：

- `tmp/pdfs/batch_031_747_752/page-520.png`
- `tmp/pdfs/batch_031_747_752/page-521.png`
- `tmp/pdfs/batch_031_747_752/page-522.png`
- `tmp/pdfs/batch_031_747_752/page-523.png`

检查结果：

- `page-520`：`#747` 圆锥通量的总通量公式和结论显示正常，未与上一批内容产生挤压或错位。
- `page-521`：`#748-#749` 的旋度定义、Stokes 公式和两道曲线积分推导层次清楚，长公式已拆出，题干与解答衔接正常。
- `page-522`：`#750-#751` 的旋度计算、空间 Stokes 两题方向说明和结果公式显示正常，无超长行内挤压。
- `page-523`：`#752` 的旋度线性证明排版稳定，后续梯度算子小节起始处无重叠或异常断行。

## 5. 验证记录

- 编译：`latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex`
  - 结果：成功生成 `elegantbook2.pdf`
- 日志扫描：`Overfull|Underfull|LaTeX Warning|Package .* Warning|Undefined control sequence|Fatal|^!`
  - 结果：无输出
- PDF 页数：`730`
- 本批索引复核：`#747-752` 的 `inline_long_count` 合计 `0`

## 6. 放行结论

批次 `#747-752` 可放行进入下一批。放行依据包括：通量、旋度和 Stokes 方法口径与本章知识点一致；方向和法向选择可追溯；计算结果复核通过；推导步骤较原解更完整；长行内公式已处理，短小公式未被机械拆出；PDF 目检未见拥挤、重叠或错位。
