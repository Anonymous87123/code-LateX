# 批次 012 阶段复核记录

生成时间：2026-06-22

## 本次范围

- 小批次：全局 `solution #382-400`
- 对应源码：`elegantbook2.tex:18644-19156`
- 处理原则：按用户确认的 `3/4 \textwidth` 口径判断长行内公式，折算阈值约为 `52.5` 个普通字符宽度；短的定义、小代入、小结论优先保留行内，系统、矩阵、主推导骨架、较长集合表达式保留行间。

## 已完成修改

- `#382-384`：回看前序改动，将短向量记号压回行内；闭集判定保留连续原像证明，避免双证法冗余。
- `#385-388`：删去“思维过程/解答”式旁白，改成教材答案式条目；拓扑集合题保留必要集合表达式，长并集用行间公式。
- `#390-392`：极限不存在题改用直角坐标路径法；可微性题明确为“下一章定义预告”，按偏导候选主部和余项商检验。
- `#393-395`：概念定义压缩，极限定义改为带蕴含的展示公式；定义域题补 `x\neq0`、`|y|\le |x|`；齐次函数题补 `y\neq0,t\neq0`。
- `#398-400`：第 (3) 极限补 `x\neq0` 定义域说明，第 (4) 改用统一夹逼；连续性反例改为直接路径 `y=x^2`；间断点集题纠正为非原点可去间断、原点无界间断。

## 自检结果

- 重建索引：`total_solutions = 1322`，结构未变。
- `solution #382-400` 的 `inline_long_count` 均为 `0`。
- 按 `52.5` 阈值额外扫描：`>=52.5` 候选 `0`，`45-52.5` 复核区候选 `0`。
- 编译：`latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex` 通过。
- 日志扫描：`Overfull|Underfull|Warning|Error|Undefined|Missing|Fatal` 零命中。
- PDF 页数：`711`。
- PDF 抽检：已渲染并查看 `tmp/pdfs/batch_012_382_400/page-312.png`、`page-315.png`、`page-316.png`、`page-317.png`、`page-318.png`；未见公式挤压、重叠或孤立引导字残留。

## 后续

- 批次 012 后半段已继续完成；覆盖全局 `solution #401-430`。

## 补充复核：`#401-430`

### 本次范围

- 小批次：全局 `solution #401-430`
- 对应源码：`elegantbook2.tex:19162-20199`
- 处理原则：继续按用户确认的 `3/4 \textwidth` 口径判断长行内公式，折算阈值约为 `52.5` 个普通字符宽度；短定义、小代入、小结论保留行内，矩阵、方程组、换元骨架和实际较长推导链保留行间。

### 已完成修改

- `#401-405`：补第一象限趋近口径、等高线/等位面空集参数情形，极限不存在题改为更直接的路径法。
- `#406-421`：补对数、根式、幂函数、分式、正切等自然定义域；可微性题统一按“候选线性主部 + 余项除以距离”核验；补全 `f_y` 不连续证明。
- `#412`：处理三元点与函数实际不含 `z` 的口径，补 `f_z(1,1,1)=0`。
- `#418`、`#420`：补完整导数表达式和数值近似结论，其中 `#420` 收口到约 `0.5023`。
- `#421`：按硬阈值复核后，仅将估宽约 `58.0` 的夹逼不等式链拆为行间式；短条件 `$|\sin(\cdot)|\leqslant 1$` 保留行内。
- `#423-430`：补向量值函数定义域、Jacobian 矩阵维数、逆映射对应点、坐标变换绝对值和参数范围。

### 自检结果

- 重建索引：`total_solutions = 1322`，结构未变。
- `solution #401-430` 的 `inline_long_count` 均为 `0`。
- 按 `52.5` 阈值额外扫描：`>=52.5` 候选 `0`。
- 编译：`latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex` 通过。
- 日志扫描：`Overfull|Underfull|LaTeX Warning|Package .* Warning|Undefined control sequence|Fatal|^!` 零命中。
- PDF 页数：`712`。
- PDF 抽检：已渲染并查看 `tmp/pdfs/batch_013_401_430/page-318.png`、`page-319.png`、`page-322.png`、`page-323.png`、`page-327.png`、`page-328.png`、`page-330.png`、`page-331.png`、`page-332.png`；重排后的 `page-328.png` 未见公式挤压、重叠或不当短公式拆出。
