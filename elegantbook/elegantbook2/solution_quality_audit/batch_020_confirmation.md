# 批次 020 问题确认台账

生成时间：2026-06-22

## 0. 执行边界

本文件只记录全局 `644-657` 号 `solution` 的人工审计结论、问题根源和拟修正口径，不修改正文 `elegantbook2.tex`。

- 批次范围：全局 `644-657`
- 源码范围：`elegantbook2.tex:22765-23059`
- 覆盖内容：曲线积分与曲面积分知识讲解开篇例题，包括第一类曲线积分、弧长元、第一类曲面积分、面积元、球面参数化、对称性和投影公式
- 前文口径依据：本章 `第一类曲线积分与弧长元`、`第一类曲面积分与面积元`、`正则曲面面积元`、`对称性`、`投影面积元`

本批次应在用户确认本台账口径后再进入正文批改。

## 1. 本批次统计

| 项目 | 数量 |
| --- | ---: |
| 本批 `solution` 数 | 14 |
| `A_必须改` | 0 |
| `B_建议改` | 8 |
| `C_可保留` | 6 |
| 明确数学/计算错误 | 0 |
| 外部图示/常量说明需补写 | 1 |
| 面积元或投影边界说明需补写 | 2 |
| 长行内公式/排版拥挤需拆分 | 7 |

说明：本批次未发现硬性计算错误。主要问题是若干例题公式过长、个别推导依赖“如图”或未定义的引力常量、上半球投影解法在赤道处除以 `z` 未说明极限处理，以及空间圆周参数式过度压缩。

## 2. 统一修正口径

本批次正文修改时统一采用以下口径：

1. 第一类曲线积分先写参数、弧长元 `ds`，再代入线密度或被积函数。
2. 第一类曲面积分先写参数曲面或投影公式，再写面积元 `dS`；若投影公式含分母为零的边界点，应说明只在内部使用并按极限延拓。
3. 引力或物理模型题必须说明常量含义、方向约定和几何半径，不依赖“如图”才能读懂。
4. 对称性判零题应明确奇偶变量和对称区域。
5. 长参数式、长叉乘式、长积分链统一拆为 display 或 `align*`。
6. 若存在更符合本章口径的简洁方法，例如利用空间对称性替代冗长参数化，可在正文整改时优先采用。

## 3. 逐题问题清单

| 全局号 | 源码行 | 严重度 | 问题分类 | 证据 | 拟修正口径 |
| ---: | --- | --- | --- | --- | --- |
| 644 | `22765-22775` | B_建议改 | 长行公式 | 质量公式、参数导数和密度计算压在较长行内 | 保留结果 `\sqrt{a^2+b^2}(2\pi a^2+\frac83\pi^3b^2)`；拆成“参数-弧长元-密度-积分”四步 |
| 645 | `22780-22786` | B_建议改 | 长计算链 | 导数、弧长元、`x^2+y^2` 化简和最终积分集中在少数长行 | 保留结果 `2\pi^2a^3(1+2\pi^2)`；将导数与被积函数化简分行 |
| 646 | `22791-22797` | B_建议改 | 常量/图示依赖；长公式 | 题面说“如图3”，解答默认半径为 `a`、上半圆和引力常量 `K`，但未集中说明 | 补“设半圆半径为 `a`，取上半圆，引力常量为 `K`”；分开写微元方向和竖直分量 |
| 647 | `22802-22831` | B_建议改 | 参数式过长；方法可优化 | 空间圆周参数式整行很长；后续又说 `y^2,z^2` 结果相同但未说明置换对称性 | 可保留参数法但拆长式；或改用平面圆周各坐标平方平均的对称法 |
| 648 | `22836-22844` | C_可保留 | 可保留 | 圆周第一类积分用奇偶性判零正确 | 无需改 |
| 649 | `22849-22854` | C_可保留 | 可保留 | 右半圆参数范围、弧长元和绝对值积分正确 | 无需改 |
| 650 | `22903-22926` | C_可保留 | 可保留 | 球面参数化、面积元和面积 `4\pi a^2` 推导完整 | 无需改 |
| 651 | `22932-22939` | C_可保留 | 可保留 | 球面上 `x^2+y^2+z^2=a^2`，结果 `4\pi a^4` 正确 | 无需改 |
| 652 | `22945-22950` | B_建议改 | 面积元计算长行 | 抛物面投影和分母消去正确，但参数方程、面积元、极坐标计算压成两行 | 保留结果 `243\pi/2`；拆出 `dS=\sqrt{1+4x^2+4y^2}\,dxdy` 与极坐标积分 |
| 653 | `22955-22960` | C_可保留 | 可保留 | 上半球壳对 `z` 轴转动惯量 `\frac43\pi a^4` 正确 | 无需改 |
| 654 | `22965-22997` | B_建议改 | 分块曲面长行 | 三块曲面积分结果正确，但每块参数、面积元和积分式都偏长 | 保留 `I_1=\pi,I_2=I_3=0`；按“侧面-顶面-底面”分 display 展示 |
| 655 | `23003-23014` | B_建议改 | 曲面积分记号；投影边界说明 | 题面和解答多处用 `\int_\Sigma` 表示曲面积分；另解中 `z\cdot a/z` 在赤道 `z=0` 处未说明按内部极限处理 | 统一为 `\iint_\Sigma`；补投影公式在开圆盘内使用、边界由极限延拓，不影响积分 |
| 656 | `23022-23044` | B_建议改 | 条件说明；面积元跳步 | Poisson 公式证明正确，但未说明 `f` 至少可积；`||r_w\times r_\theta||=1` 可补一行计算 | 补 `f` 的可积/连续条件；将新坐标、截面参数化和面积元计算分行 |
| 657 | `23050-23059` | C_可保留 | 可保留 | 平面截球面区域的坐标旋转、投影半径和结果 `\frac{\pi}{18}(3-t^2)^2` 正确 | 无需改 |

## 4. 本批次重点返工项

优先返工顺序建议：

1. 全局 `655`：统一第一类曲面积分记号，并补赤道处投影公式的极限说明。
2. 全局 `646`：补半圆半径、方向和引力常量说明，避免依赖图示。
3. 全局 `647`：拆超长空间圆周参数式，必要时改成对称性解法。
4. 全局 `656`：补 `f` 的条件和面积元计算。
5. 其余 `B_建议改` 项：以拆长公式和补面积元账本为主。

## 5. 放行前自检清单

正文修改后必须逐项自检：

- 第一类曲线积分是否写清参数范围和 `ds`。
- 第一类曲面积分是否写清参数范围和 `dS`。
- 使用对称性判零时是否说明对应变量和对称区域。
- 投影面积元是否在分母可能为零处作极限说明。
- 物理模型题是否说明常量、方向和几何参数。
- 所有长行公式是否拆为 display 或 `align*`。
