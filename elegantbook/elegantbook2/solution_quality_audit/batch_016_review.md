# 批次 016 整改复审记录

生成时间：2026-06-23

## 0. 执行边界

- 批次范围：全局 `556-586`
- 源码范围：`elegantbook2.tex:25021-26295`
- PDF 复核页：物理页 `402-423`（`#586` 解答延至 `423` 页上半；`423` 页下半已进入下一批 `#587`）
- 本批主题：重积分知识讲解中的二重积分、反常二重积分、三重积分、柱坐标、球坐标、物理应用与一般换元。
- 本轮收口原则：不继续向 `#587+` 扩展；只对本批已经按审计报告整改后的内容做闭环复核。

## 1. 本批整改口径

本批按用户重新钉死的公式排版标准执行：满行约折算为 `70` 个普通字符宽度，只有行内公式折算宽度接近 `3/4\textwidth`，即 `>=52.5` 时，才作为必须拆出的硬问题；`45-52.5` 只进入人工复核区。短定义、小代入、小结论、局部提示式公式优先保留行内，不机械拆成行间公式。

内容上同步检查三件事：

1. 推导链是否完整，是否存在跳步或只给结论。
2. 方法是否与本节前文的 Fubini、区域分类、极坐标、柱坐标、球坐标、反常积分、Jacobian 等口径一致。
3. 计算结论是否正确，反常积分与奇点是否补足合法性说明。

## 2. 已完成的主要正文整改

- `#556` 补明黎曼分割小块需有非空内部，分别用有理点与非有理点采样，两个黎曼和极限 `1` 与 `0` 形成定义反例。
- `#559` 将中值点极限改为夹逼论证：由 `(\xi_\rho,\eta_\rho)\in D_\rho` 得中值点趋于原点，再用连续性收口。
- `#561` 补两相切圆的几何结构、端点重合说明，完整列出 `x` 型三块与 `y` 型两块区域。
- `#562` 补第二种积分次序的实际计算，验证结果仍为 `11/120`。
- `#563` 将题名与文字口径改为“非初等原函数下的积分次序选择”，避免把 `e^{y^2}` 误称为不可积函数。
- `#564` 修正 `\mathrm \mathrm{d}` 记号错误，分部积分步骤显示为可检查链条。
- `#567` 补 `a>0`，并按内切圆、正方形、外接圆夹逼推导概率积分。
- `#568` 补两圆与直线 `y=x` 的交点和极角范围来源。
- `#570` 补非负性/Tonelli 或截断极限说明，保证全平面 Gaussian 积分分离合法。
- `#571` 补绝对收敛说明 `|\min(x,y)|\le |x|+|y|`，再拆分 `D_1,D_2`。
- `#573` 补椭球对称性消去交叉项，并用截面/轮换坐标计算三项。
- `#575` 题名改为偏心球的球坐标积分，与实际区域一致。
- `#580`、`#586` 补奇点截断 `\varepsilon` 与极限过程。
- `#581` 补新区域边界在 `uv` 平面的来源和 Jacobian 计算。
- `#582` 补连续函数存在原函数的依据，再用奇偶性处理抽象函数项。
- `#585` 将题面与解答统一为 `t\to0^+`，避免负半径口径。

## 3. 逐题内容复核摘要

| 全局号 | 复核结论 |
| ---: | --- |
| 556 | 不可积反例逻辑完整；两种采样策略均依赖稠密性，结论正确。 |
| 557 | 上半球体积解释正确，结果 `2\pi R^3/3` 正确。 |
| 558 | 估值定理应用、椭圆面积和最大最小值判断正确。 |
| 559 | 中值定理与连续性桥接完整，极限为 `f(0,0)`。 |
| 560 | 矩形区域累次积分正确，结果 `21/2`。 |
| 561 | `x` 型、`y` 型区域划分均正确，端点重合处理不影响积分。 |
| 562 | 两种次序计算一致，结果 `11/120`。 |
| 563 | 积分次序选择合理，质量 `M=e-1` 正确。 |
| 564 | 换序区域与分部积分正确，结果 `1-\sin1`。 |
| 565 | 对称性消去 `y` 项，补集计算正确，结果 `4\pi-8/15`。 |
| 566 | 极坐标 Jacobian 推导与面积微元一致。 |
| 567 | 圆域积分与 Gaussian 夹逼证明正确。 |
| 568 | 极坐标区域与结果 `14a^3(8-5\sqrt2)/9` 正确。 |
| 569 | 非负广义积分累次计算正确，结果 `1/2`。 |
| 570 | 全平面 Gaussian 积分为 `\pi`，概率积分结论正确。 |
| 571 | 绝对收敛、区域拆分和对称换元正确，结果 `-\sqrt{2\pi}/2`。 |
| 572 | 四面体积分 `I=J=1/60` 正确。 |
| 573 | 椭球三项轮换结果正确，合并为 `4\pi abc(a^2+b^2+c^2)/15`。 |
| 574 | 抛物面/球面交线、柱坐标限和结果 `13\pi/4` 正确。 |
| 575 | 偏心球球坐标限正确，结果 `32\pi/15`。 |
| 576 | 牟合方盖按第一卦限切片正确，体积 `16/3`。 |
| 577 | 圆锥面与抛物面夹层体积 `32\pi/3` 正确。 |
| 578 | 质心计算完整，结果 `(0,0,2/3)`。 |
| 579 | 半圆薄片对直径轴转动惯量 `\pi a^4/8` 正确。 |
| 580 | 引力奇点截断后径向因子抵消，向量 `(0,0,\pi K R\sin^2\alpha)` 正确。 |
| 581 | 换元 `u=x+y,v=y`、新区域与结果 `198/5-10\sqrt5/3` 正确。 |
| 582 | 原函数法与奇偶性消项正确，结果 `-2/5`。 |
| 583 | 心脏线内圆外区域极坐标正确，结果 `22/9+\pi/2`。 |
| 584 | 柱坐标积分正确，结果 `256\pi/3`。 |
| 585 | 球坐标积分与洛必达法则合法，结果 `\pi f'(0)`。 |
| 586 | 轴上可积奇点处理完整，结果 `2\pi e^2`。 |

本轮复核未发现本批仍残留硬性计算错误、逻辑断裂或与前文方法口径冲突的问题。

## 4. 公式排版复核

按折算阈值扫描源码范围 `25021-26295`：

- `inline_math_candidates_ge45 = 5`
- `hard_ge52_5 = 0`
- `near_45_52_5 = 5`

人工复核区的 5 个候选均未达到硬拆阈值，且属于一元积分小计算链、极限小结论或局部拆分式；PDF 中可读性正常，因此保留为行内或随正文排版，不强行改成行间公式。

## 5. PDF 版面复核

已渲染并抽检：

- 总览图：`tmp/pdfs/batch_016_556_586/contact_1.png`、`contact_2.png`、`contact_3.png`
- 单页图：`page-402.png` 至 `page-423.png`
- 重点页：`406`、`407`、`412`、`419`、`420`、`422`、`423`

检查结果：

- `#561` 的区域集合显示正常，没有横向溢出。
- `#562` 的第二积分次序验证公式已分行，版面正常。
- `#571` 的绝对收敛说明、分区积分和结果没有拥挤。
- `#580`、`#586` 的奇点截断推导清晰，未出现公式压边。
- `#585` 的 `t\to0^+` 口径在题面与解答中一致。

## 6. 验证记录

- 重建索引：`python solution_quality_audit/build_solution_index.py`
  - `total_solutions = 1322`
- 编译：`latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex`
  - 结果：PDF 已是最新，目标均 up-to-date
- 日志扫描：`Overfull|Underfull|LaTeX Warning|Package .* Warning|Undefined control sequence|Fatal|^!`
  - 结果：无输出
- PDF 页数：`718`
- `git diff --check -- elegantbook2.tex solution_quality_audit/solution_index.csv`
  - 结果：仅有既有 CRLF 提示，无空白错误

## 7. 放行结论

批次 `#556-586` 可放行进入下一批。放行理由不是“公式不长”，而是本批同时通过了解答正确性、步骤完整性、方法口径一致性、反常/奇点合法性、PDF 版面与编译日志检查。
