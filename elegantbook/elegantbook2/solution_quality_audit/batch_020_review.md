# 批次 020 整改复审记录

生成时间：2026-06-23

## 0. 执行边界

- 批次范围：全局 `#644-657`
- 源码范围：`elegantbook2.tex:28689-29069`
- PDF 复核页：物理页 `459-465`，其中正文主检查页为 `460-464`，对应印刷页 `410-414`
- 本批主题：曲线积分与曲面积分知识讲解开篇例题，包括第一类曲线积分、弧长元、第一类曲面积分、面积元、球面参数化、对称性和投影公式。
- 本轮原则：按已确认审计报告修改当前批次；内容复核优先检查对象识别、参数或面积元、对称性依据、投影公式边界条件和计算结果。公式排版继续采用用户指定的 `3/4\textwidth` 口径，硬阈值为 `52.5/70` 折算单位。

## 1. 已完成的主要正文整改

- `#644`：将质量计算拆为“参数、弧长元、线密度、积分”四步，保留结果 `\sqrt{a^2+b^2}(2\pi a^2+\frac83\pi^3b^2)`。
- `#645`：拆出导数、弧长元和 `x^2+y^2` 化简，避免把导数、面积元和积分链压在一行。
- `#646`：补明半圆半径为 `a`、取上半圆、`K` 为引力常量，并从微元质量、距离和方向推出引力微元；去除图示依赖。
- `#647`：改用平面圆周与坐标置换对称性求二次矩，避免长空间参数式；结果仍为 `2\pi a^3/3`。
- `#652`：补出图形曲面投影域、`dS=\sqrt{1+4x^2+4y^2}\,dxdy` 和极坐标积分，保留结果 `243\pi/2`。
- `#654`：按“侧面、顶面、底面”分块展示面积元和积分；顶面、底面用对称性判零。
- `#655`：统一第一类曲面积分记号为 `\iint_\Sigma`；补充投影公式在赤道 `z=0` 处按内部极限理解，边界面积为零，不影响积分。
- `#656`：补充 `f` 至少可积或连续的条件；展开 `E,G,F` 面积元计算，说明端点参数退化按极限延拓不影响积分。
- `#657`：统一曲面积分记号为 `\iint`；补明正交坐标变换不改变平面面积元，修正最后极坐标积分的积分次序书写。

## 2. 逐题内容复核摘要

| 全局号 | 复核结论 |
| ---: | --- |
| 644 | 第一类曲线积分质量公式、弧长元 `\sqrt{a^2+b^2}` 和密度 `a^2+b^2t^2` 均正确，结果复算通过。 |
| 645 | 导数 `x'=at\cos t,y'=at\sin t`、弧长元 `at\,dt` 和 `x^2+y^2=a^2(1+t^2)` 正确，结果 `2\pi^2a^3(1+2\pi^2)` 复算通过。 |
| 646 | 引力微元方向、距离和密度说明完整；水平分量由对称性抵消，竖直分量 `2K/a` 正确。 |
| 647 | 圆周半径为 `a`，坐标置换对称性使三个二次矩相等；由总和 `a^2\cdot2\pi a` 得 `2\pi a^3/3`，比原参数法更贴合本章口径。 |
| 648 | 圆周第一类积分用奇偶性判零正确，保留。 |
| 649 | 右半圆参数范围、弧长元和绝对值积分正确，PDF 中未见拥挤，保留。 |
| 650 | 球面参数化、`E,G,F` 和面积元 `a^2\sin\phi` 推导完整，保留。 |
| 651 | 球面上被积函数恒为 `a^2`，面积因子正确，结果 `4\pi a^4` 复算通过。 |
| 652 | 抛物面投影、面积元和分母相消逻辑正确，极坐标积分结果 `243\pi/2` 复算通过。 |
| 653 | 上半球壳对 `Oz` 轴转动惯量公式正确，结果 `4\pi a^4/3` 保留。 |
| 654 | 三块曲面分解正确；侧面得 `\pi`，顶面和底面由奇函数在圆盘对称域上积分为零。 |
| 655 | 上半球对称性判零和球坐标计算正确；投影法已补赤道边界极限说明，结果 `\pi a^3`。 |
| 656 | Poisson 公式证明使用正交坐标旋转和按 `w` 分层；面积元 `dS=dw\,d\theta` 推导完整，结论成立。 |
| 657 | 平面截球得到的平面圆盘半径、面积元和极坐标积分均正确，结果 `\pi(3-t^2)^2/18` 复算通过。 |

本轮复核未发现本批仍残留的硬性计算错误、面积元错误、结论偏差或与本章第一类曲线/曲面积分方法口径冲突的问题。

## 3. 公式排版复核

按校准后的折算扫描源码范围 `28689-29070`：

- `inline_math_candidates_ge45 = 2`
- `hard_ge52_5 = 0`
- `near_45_52_5 = 2`

两个 near 项为：

- 定义区中的点积表达式 `F=\vec r_u\cdot\vec r_v=...`，低于硬阈值，且为定义公式，不在本批 solution 主体中。
- `#652` 题干中的曲面积分式，低于硬阈值，PDF 页面未见挤压。

索引启发式还提示 `#649` 和 `#657` 的长行风险。PDF 目检确认这两处只是正常换行，不存在接近 `3/4\textwidth` 的硬挤压；且它们属于参数范围说明和坐标旋转说明，未机械拆为行间公式。

## 4. PDF 版面复核

已重新渲染并检查：

- `tmp/pdfs/batch_020_644_657/page-459.png` 至 `page-465.png`

检查结果：

- `page-460`：`#644-646` 开始处显示正常，参数、弧长元、密度和引力微元公式未越界。
- `page-461`：`#646` 计算结尾、`#647` 对称性解法、`#648-649` 版面正常；`#649` 未见拥挤。
- `page-462`：曲面积分定义和 `#650-652` 开始处显示正常；`#652` 题干公式未越界。
- `page-463`：`#652` 计算结尾、`#653-655` 开始处显示正常；分块曲面积分层次清楚。
- `page-464`：`#655-657` 显示正常；赤道极限说明、Poisson 面积元计算和正交坐标面积元说明清楚。
- `page-465`：下一小节边界页显示正常，无本批修改引起的节切换问题。

## 5. 验证记录

- 重建索引：`python solution_quality_audit/build_solution_index.py`
  - `total_solutions = 1322`
  - `long_inline_flagged = 243`
- 编译：`latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex`
  - 结果：成功生成 `elegantbook2.pdf`
- 日志扫描：`Overfull|Underfull|LaTeX Warning|Package .* Warning|Undefined control sequence|Fatal|^!`
  - 结果：无输出
- PDF 页数：`722`
- `git diff --check -- elegantbook2.tex solution_quality_audit/solution_index.csv solution_quality_audit/summary.json solution_quality_audit/solution_quality_ledger.md`
  - 结果：仅有既有 Windows 换行提示，无空白错误

## 6. 放行结论

批次 `#644-657` 可放行进入下一批。放行依据包括：逐题数学复算通过、弧长元和面积元账本补齐、图示依赖和常量说明缺口已修正、第一类曲面积分记号统一、投影边界极限说明补齐、硬长行内公式清零、PDF 页面目检通过。
