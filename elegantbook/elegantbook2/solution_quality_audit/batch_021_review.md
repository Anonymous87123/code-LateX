# 批次 021 整改复审记录

生成时间：2026-06-23

## 0. 执行边界

- 批次范围：全局 `#658-673`
- 源码范围：`elegantbook2.tex:29127-29712`
- PDF 复核页：物理页 `465-476`，其中正文主检查页为 `465-473`，对应印刷页 `415-423`
- 本批主题：第二类曲线积分、端点差、闭曲线定向、奇点合法性、保守场判别、势函数、Green 公式与二维 Green 第一/第二公式证明。
- 本轮原则：内容复核与排版复核并重；先核对定义域、方向、奇点、保守场条件、Stokes 定向和 Green 符号链，再按用户指定的 `3/4\textwidth` 口径处理真正过长的行内公式。硬阈值为 `52.5/70` 折算单位。

## 1. 已完成的主要正文整改

- `#658`：拆出全微分识别、端点差和同圆周端点三步；去掉图示依赖，物理解释改为径向力与切向位移垂直。
- `#660`：补明被积式在原点无定义，按曲线不经过原点、位于去心上半平面理解，再使用端点差。
- `#662`：补充原点为奇点且位于单位圆内部，不能直接在整个圆盘上套 Green 公式。
- `#663`：拆出椭圆正向参数、微分、代入积分和整周期三角积分为零的理由。
- `#664`：补明 `z` 轴向上、重力场为 `(0,0,-mg)`，功只与高度差有关。
- `#665`：按本章“空间闭曲线先查 Stokes”的口径重写第二个积分；取指向第一卦限的单位法向，复算结果为 `\sqrt3\pi a^2`。
- `#667`、`#669`：将超过硬阈值的端点差计算链改为行间公式；低于阈值的小代入链保留行内。
- `#670-673`：重写 Green 第一、第二公式的左右两向证明，补齐外法向与切向换算、`P,Q` 选取、Green 公式代入、乘积求导展开和边界项替换。
- 附带修正 Green 公式定理本体：将条件从误写的 `u,v` 改为 `P,Q` 连续一阶偏导；再单独说明取 `P=0,Q=uv` 的特例。

## 2. 逐题内容复核摘要

| 全局号 | 复核结论 |
| ---: | --- |
| 658 | `x dx+y dy=\frac12 d(x^2+y^2)`，两端点在同一圆周上，结果 `0` 正确；物理解释也与径向/切向垂直一致。 |
| 659 | 折线 `OAB` 两段积分各为 `2/3`，直线 `OB` 积分为 `8/3`，体现路径相关性，计算正确。 |
| 660 | 在去心定义域内 `d\sqrt{x^2+y^2}` 可用，端点模长为 `1` 与 `10`，结果 `9` 正确。 |
| 661 | 直线段参数方向从 `A` 到 `B`；等价于精确微分端点差，结果 `-49/2` 正确。 |
| 662 | 单位圆正向参数化得到 `2\pi`；奇点说明完整，未误用 Green。 |
| 663 | 椭圆参数方向为逆时针，代入后 `cos2\theta` 与 `sin2\theta` 整周期积分为零，结果 `0` 正确。 |
| 664 | 重力场方向、参数方向和高度差符号一致，`W=-mg(z_2-z_1)` 正确。 |
| 665 | 第一问由球面约束得 `p\cdot dp=0`；第二问用 Stokes，旋度 `(1,1,1)`、法向 `(1,1,1)/\sqrt3`、面积 `\pi a^2`，结果 `\sqrt3\pi a^2` 正确。 |
| 666 | 定义域为全空间，三组交叉偏导相等，势函数 `xyz+C` 正确。 |
| 667 | 全平面保守场，势函数 `(x^2+y^2)/2`，端点差 `23/2` 正确。 |
| 668 | 全平面单连通，`P_y=Q_x=\cos x`，势函数 `y\sin x+C` 正确。 |
| 669 | 保守场条件成立，势函数 `x^2/2+xy^2-8y+C`，功 `-23/2` 正确。 |
| 670 | Green 第一公式左推右：外法向、法向导数、`P=-vu_y,Q=vu_x` 与乘积求导链完整，符号正确。 |
| 671 | Green 第一公式右推左：先凑区域积分，再由 Green 回到边界项，最后替换为法向导数，逻辑闭合。 |
| 672 | Green 第二公式左推右：`v\Delta u-u\Delta v` 展开、抵消项和边界法向导数替换均正确。 |
| 673 | Green 第二公式右推左：`Q_x-P_y` 的减号来源已拆清，最后回到 `v\Delta u-u\Delta v`，符号正确。 |

本轮复核未发现本批残留的计算错误、结论偏差、奇点误用、定向错误或与本章方法口径冲突的问题。

## 3. 公式排版复核

按校准后的折算扫描源码范围 `29127-29725`：

- `inline_math_candidates_ge45 = 1`
- `hard_ge52_5 = 0`
- `near_45_52_5 = 1`

剩余 near 项为 `#669` 中 `2xy+\varphi'(y)=2xy-8,\ \varphi'(y)=-8,\ \varphi(y)=-8y+C`，估算宽度约 `48.3/70`，低于硬阈值，且属于求势函数中的小代入链，按用户口径保留行内。

重建索引后：

- `total_solutions = 1322`
- `long_inline_flagged = 240`
- 本批 `#658-673` 的 `inline_long_count` 均为 `0`

## 4. PDF 版面复核

已重新渲染并检查：

- `tmp/pdfs/batch_021_658_673/page-465.png` 至 `page-476.png`

检查结果：

- `page-465`：第二类曲线积分定义与 `#658` 开始处显示正常。
- `page-466`：`#658-662` 的端点差、路径分段、奇点说明和单位圆参数化显示正常。
- `page-467`：`#663-665` 开始处显示正常，椭圆、重力和球面平面交线题无拥挤。
- `page-468`：`#665` Stokes 解法和保守场入口页显示正常。
- `page-469`：`#666-669` 显示正常，端点差长链拆分后版面自然。
- `page-470`：Green 公式定理条件、`P=0,Q=uv` 特例和做题流程显示正常。
- `page-471`：Green 第一公式定理与左推右证明显示正常。
- `page-472`：Green 第一公式右推左收口与 Green 第二公式开头显示正常。
- `page-473`：Green 第二公式右推左证明显示正常；下一练习仅作为边界页检查。

## 5. 验证记录

- 重建索引：`python solution_quality_audit/build_solution_index.py`
  - `total_solutions = 1322`
  - `long_inline_flagged = 240`
- 编译：`latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex`
  - 结果：成功生成 `elegantbook2.pdf`
- 日志扫描：`Overfull|Underfull|LaTeX Warning|Package .* Warning|Undefined control sequence|Fatal|^!`
  - 结果：无输出
- PDF 页数：`723`
- `git diff --check -- elegantbook2.tex solution_quality_audit/solution_index.csv solution_quality_audit/summary.json solution_quality_audit/solution_quality_ledger.md`
  - 结果：仅有既有 Windows 换行提示，无空白错误

## 6. 放行结论

批次 `#658-673` 可放行进入下一批。放行依据包括：逐题复算通过、定义域/奇点/方向条件补齐、Stokes 与 Green 方法口径和前文一致、Green 公式证明符号链完整、硬长行内公式清零、PDF 页面目检通过。
