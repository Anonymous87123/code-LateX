# 批次 015 复审记录

## 范围

- 全局 `solution`：`#523-555`
- 源码范围：`elegantbook2.tex:23861-24950`
- PDF 正文页：物理页 `385-400`，对应正文页码 `335-350`
- 过渡页：物理页 `401`（第 4 章“重积分”起始页）已检查

## 本批排版口径

行内公式拆分按用户确认的 `3/4\textwidth` 标准执行：一行满宽按约 `70` 个普通字符宽度估算，硬拆分阈值取 `52.5` 折算单位。`45-52.5` 只作为人工复核区间，不机械拆分；短定义、小代入、小结论、低层级说明优先保留为行内。

本批复扫结果：

- `inline_math_candidates_ge45 = 8`
- `hard_ge52_5 = 0`
- `near_45_52_5 = 8`

其中最后一次复扫前发现 4 条 `>=52.5` 的硬超限行内公式，已只针对这 4 条改成行间公式；其余 near 项经 PDF 视觉复核后保留行内。

## 主要整改

- `#523-524`：补足复合函数替换、隐函数导数取值点、局部分支条件；修正最大体积推导，改用 `s=x^2/a^2,t=y^2/b^2` 得 `s=t=1/4`。
- `#526`：修正单选题冲突，将选项限定为真正单选。
- `#529-540`：补充可微反例说明、隐函数可解条件、定义域分支、抽象偏导取值点，并按链式法则口径展开。
- `#541-546`：补闭区域边界检查、完成隐函数极值题、补二次优化边界、修正椭圆面积最小化和 AM--GM 证明中的退化/边界情况。
- `#548-555`：补正八分球边界行为、线面角公式、正则点条件、切平面矛盾证明和切平面过定点证明。
- 按 `52.5` 阈值额外处理 4 条硬超限行内链式公式：曲面夹角余弦、隐函数恒等式验证、`z'` 隐导链、二阶偏导结果组。

## 内容复核

- `#542`：数值复核方程 `y^5-y^4-9y^2+36y-36=0` 只有一个实根 `y_0≈1.4864`，对应 `x_0≈-4.3016,z_0≈-1.3288`；二阶导数 `z_{xx}≈-0.1521,z_{xy}≈0.6728,z_{yy}≈1.3455`，判别式约 `-0.6573<0`，故为鞍点，无极值。
- `#543`：内部驻点 `(173/7,-340/7)` 在区域外，边界最大值复核为 `g(85,75)=33625`。
- `#550`：线面角使用 `sin 30^\circ`，得到平面常数 `\pm 7R/2`。
- `#553`：直线代入切平面条件推出 `z_0=-14`，与椭球方程矛盾，故无实切平面。

## 验证

- `python solution_quality_audit/build_solution_index.py`：`total_solutions=1322`
- `latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex`：通过，生成 `elegantbook2.pdf`
- `pdfinfo elegantbook2.pdf`：`Pages: 717`
- 日志筛查 `Overfull|Underfull|LaTeX Warning|Package .* Warning|Undefined control sequence|Fatal|^!`：无命中
- `git diff --check -- elegantbook2.tex solution_quality_audit/solution_index.csv`：仅有预期 CRLF 提示
- PDF 渲染检查：`tmp/pdfs/batch_015_523_555/page-385.png` 至 `page-401.png`

## 结论

本批 `#523-555` 已按审计报告完成正文整改、公式阈值复扫、编译验证和 PDF 视觉复核。本记录只覆盖本批次，不代表全书整改已完成。
