# 批次 032 整改复审记录

生成时间：2026-06-23

## 0. 执行边界

- 批次范围：全局 `#753-758`
- 源码范围：`elegantbook2.tex:33126-33345`
- PDF 复核页：物理页 `523-526`，并查看 `527` 作为下批过渡页
- 本轮主题：梯度算子基本公式、旋度/散度恒等式、本章总习题补充题。
- 本轮复核口径：同步核对公式证明条件、乘法求导展开、Gauss 封口方向、路径无关条件、第一类曲线积分参数化和计算结论。排版上只拆真正过长或承担主计算骨架的公式，短变量定义、短小散度结论和局部代入保留行内。

## 1. 已完成的主要整改

- `#756`：补上 `A_1,A_2,A_3` 具有二阶连续偏导数的前提，并明确三组混合偏导相等关系。
- `#757`：将原来的长行内散度公式拆出；补充乘法求导法则的逐项展开，再分组得到目标恒等式，避免从“逐项展开”直接跳到结论。
- `#758`：把第 1、2、3、4、5 小问中的长主计算公式改为行间展示；第 2 问中短小散度结论改回行内 `P_x+Q_y+R_z=1`，避免不必要的行间化。
- `#753-755`：公式结构与计算结论已人工复核，未作不必要改写；其中保留的短 display 属于公式证明骨架。

## 2. 逐题内容复核摘要

| 全局号 | 复核结论 |
| ---: | --- |
| 753 | `\nabla\cdot(C\vec A)=C\nabla\cdot\vec A` 由常数提出偏导得到；`\nabla\cdot(u\vec C)=\nabla u\cdot\vec C` 在 `\vec C=(a,b,c)` 为常向量时成立，方法与散度定义一致。 |
| 754 | 由 `\nabla u=(u_x,u_y,u_z)` 直接取散度，得到 `u_{xx}+u_{yy}+u_{zz}`，正是 `\Delta u` 的定义，结论正确。 |
| 755 | 设 `f` 有二阶连续偏导，`\nabla\times(\nabla f)` 的三个分量分别是混合偏导差，利用混合偏导可交换得零向量，结论正确。 |
| 756 | 已补齐二阶连续偏导前提。展开 `\nabla\cdot(\nabla\times\vec A)` 后三对项分别由 `A_{3yx}=A_{3xy}`、`A_{2zx}=A_{2xz}`、`A_{1zy}=A_{1yz}` 抵消，结论为 `0`。 |
| 757 | 叉积为 `(QN-RM,RL-PN,PM-QL)`；散度逐项用乘法法则展开后，按 `L,M,N` 和 `P,Q,R` 分组，正好得到 `\vec G\cdot\operatorname{rot}\vec F-\vec F\cdot\operatorname{rot}\vec G`。 |
| 758 | 五个补充题逐项复核通过：扇形边界第一类曲线积分为 `2(e^a-1)+\pi a e^a/4`；闭曲面第二型曲面积分由 Gauss 化为体积 `2\pi`；路径无关给出 `\phi(x)=x^2` 和积分 `1/2`；锥面下侧用封口 Gauss 得 `3\pi/2`；平面大圆参数化后积分为 `2\pi`。 |

本轮未发现结论性计算错误；主要问题是 `#757` 证明跳步和 `#758` 多处主计算行内过长。已同步修正证明完整性和排版。

## 3. 公式排版复核

- 重建索引：`python solution_quality_audit/build_solution_index.py`
  - `total_solutions = 1322`
  - `long_inline_flagged = 195`
  - `short_display_flagged = 762`
- 本批 `#753-758` 的 `inline_long_count` 全部为 `0`。
- `#753`、`#755`、`#757`、`#758` 仍有短 display 候选，但逐项判断后均为公式证明骨架、乘法展开、Gauss 主等式、体积计算、封口扣除或参数化主积分，不属于应压回行内的短变量定义。
- 第 2 问散度 `P_x+Q_y+R_z=1` 已保留为行内短公式，避免过度 display 化。

## 4. PDF 版面复核

已渲染并目检：

- `tmp/pdfs/batch_032_753_758/page-523.png`
- `tmp/pdfs/batch_032_753_758/page-524.png`
- `tmp/pdfs/batch_032_753_758/page-525.png`
- `tmp/pdfs/batch_032_753_758/page-526.png`
- `tmp/pdfs/batch_032_753_758/page-527.png`

检查结果：

- `page-523`：`#753-#756` 梯度与旋度公式证明显示正常，无拥挤或错位；`#756` 新增前提与后续推导衔接自然。
- `page-524`：`#757` 乘法展开分行后宽度合适，证明层次清楚；本章总习题引导段与补充题题干衔接正常。
- `page-525`：`#758` 第 1-4 小问主计算公式分行后可读；第 2 问散度短结论改回行内后未再出现正文断公式问题。
- `page-526`：`#758` 第 4、5 小问封口扣除、正交基参数化、最终积分显示正常，无重叠、溢出或异常断行。
- `page-527`：下批内容起始页过渡正常。

## 5. 验证记录

- 编译：`latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex`
  - 结果：成功生成 `elegantbook2.pdf`
- 日志扫描：`Overfull|Underfull|LaTeX Warning|Package .* Warning|Undefined control sequence|Fatal|^!`
  - 结果：无输出
- PDF 页数：`730`

## 6. 放行结论

批次 `#753-758` 可放行进入下一批。放行依据包括：恒等式证明条件补齐、`div(F\times G)` 证明步骤完整、补充题五问计算和方向复核通过、本批长行内公式清零、短小公式未被机械拆出、PDF 目检无拥挤或错位。
