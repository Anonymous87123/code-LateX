# 批次 003 复核记录

生成时间：2026-06-22

## 批次范围

- 全局序号：`41-82`
- 当前源码范围：`elegantbook2.tex:2411-3373`
- 题型：`\sqrt{x^2+a^2}`、`\sqrt{x^2-a^2}`、`\sqrt{a^2-x^2}` 三组根式积分

## 整改状态

当前正文已经具备整改后的版本，并已按 `batch_003_confirmation.md` 的重点项复核：

- 已移除全局 `51`、`53` 的 Green/面积法串台口径，改回根式积分底座方法。
- 已删除“由此可得所求结果”“换元计算”“化简为对数形式”和 `\dots` 等占位式写法。
- 已将长行内公式和长连等式拆为 `align*` 或独立 display。
- 三组根式分别使用：
  - `y=\sqrt{x^2+a^2}`，`y^2-x^2=a^2`，`x\,dx=y\,dy`；
  - `y=\sqrt{x^2-a^2}`，`x^2-y^2=a^2`，`x\,dx=y\,dy`；
  - `y=\sqrt{a^2-x^2}`，`x^2+y^2=a^2`，`x\,dx=-y\,dy`。
- 全局 `64`、`78` 的高次根式递推链已经重写为四倍递推，未再出现错误的 `1/3` 系数链。

## 主端数学核验

已对重点母式做符号求导抽检，结果均回到原 integrand：

- 全局 `41`：`\ln(x+\sqrt{x^2+a^2})`
- 全局 `42`：`x/(a^2\sqrt{x^2+a^2})`
- 全局 `47`：`\frac1a\ln|(\sqrt{x^2+a^2}-a)/x|`
- 全局 `48`：`-\sqrt{x^2+a^2}/(a^2x)`
- 全局 `64`：`\frac{x}{8}(2x^2-5a^2)\sqrt{x^2-a^2}+\frac{3a^4}{8}\ln|x+\sqrt{x^2-a^2}|`
- 全局 `70`：`x/(a^2\sqrt{a^2-x^2})`
- 全局 `75`：`\frac1a\ln|(a-\sqrt{a^2-x^2})/x|`
- 全局 `78`：`\frac{x}{8}(5a^2-2x^2)\sqrt{a^2-x^2}+\frac{3a^4}{8}\arcsin(x/a)`
- 全局 `82`：`-\sqrt{a^2-x^2}/x-\arcsin(x/a)`

## 主端源码核验

已检查当前源码窗口 `elegantbook2.tex:2411-3373`：

- 未发现 `Green`、`面积法` 串台词。
- 未发现 `由此可得`、`换元计算`、`化简为对数形式`、`\dots` 占位词。
- 未发现长度超过 `180` 字符的长行。
- 全局 `64`、`78` 的递推式分别为正确的
  `4I_2=xy^3-3a^2I_1` 与 `4I_2=xy^3+3a^2I_1`。

## 当前批次结论

- 正文整改状态：通过
- 数学抽检：通过
- 源码结构抽查：通过
- 批次放行：已放行

说明：本次复核发现 `41-82` 当前正文已经是整改后状态，因此未再对 `elegantbook2.tex` 做重复 patch。后续进入全局 `83-120` 的正文整改。
