# 批次 018 整改复审记录

生成时间：2026-06-23

## 0. 执行边界

- 批次范围：全局 `#613-631`
- 源码范围：`elegantbook2.tex:27277-28142`
- PDF 复核页：物理页 `438-450`，另查看 `437` 作为上一批边界页
- 本批主题：三重积分的直角坐标、柱坐标、球坐标化累次积分；三重积分计算；体积、质心、转动惯量和引力模型。
- 本轮原则：按已确认审计报告修改当前批次；公式排版继续采用用户指定的 `3/4\textwidth` 口径，硬阈值为 `52.5/70` 折算单位。短定义、小代入、小结论不机械拆成行间公式；解答正确性、推导完整性和方法口径同步复核。

## 1. 本批整改口径

内容复核优先检查：

1. 三重积分区域是否先说明投影、截面、交线或锥角来源，再写积分限。
2. 柱坐标、球坐标是否完整写出变量代换和 Jacobian，避免用 `\dots` 省略被积函数变量。
3. 体积、质心、转动惯量、引力题是否使用对应标准公式，并说明对称性、方向、奇点或反常积分合法性。
4. 每个数值结果均复算，特别复核审计报告中指出的 `#627` 第 (1) 小题体积错误。

公式排版只处理两类：

- 折算宽度 `>=52.5` 的硬超限行内公式，转为 display 或改写成短行内片段。
- 短变量定义、小结论和未到硬阈值的 near 项，结合 PDF 实际版面人工判断后保留行内。

## 2. 已完成的主要正文整改

- `#613` 去除带修订痕迹的小标题措辞，保留直角坐标下各区域投影与积分限说明。
- `#615` 补全球坐标三变量代换和体积元；空间极径统一用 `\rho`；四个累次积分中不再使用 `\dots` 省略 `f` 的变量。
- `#616` 展开四面体积分、标准单纯形积分、八分之一球对称性和抛物面切片计算链。
- `#617` 保留按题干得到的 `672\pi`，去掉正文中的参考答案争议说明；第 (4) 小题补明 `z\ge0` 圆锥面与底面 `z=0` 形成封闭体。
- `#619` 补 `a>0`、`a,b,c>0` 等参数条件，补绝对值分段积分中的 `u=\cos\varphi` 步骤，并将过长坐标定义改写为短行内片段。
- `#620` 将硬超限的三重积分对称等式改为行间公式，证明结构仍按“变量对调 + 截面法”。
- `#621` 对五个体积题补交线、上下界和中间积分步骤，尤其第 (5) 小题补内外球边界比较。
- `#622` 补参数条件，展开面积、静力矩和质心比值计算。
- `#627` 修正第 (1) 小题体积：中层积分为 `\frac{(1-x)^2}{2(1+x)}`，最终结果改为 `2\ln2-\frac54`；第 (2) 小题体积链改为行间公式。
- `#629` 补 `f(x)\ge0` 或以 `|f(x)|` 为旋转半径的说明，并将硬超限的圆盘截面二重积分改为行间公式。
- `#630` 补 `K` 为引力常量，并修正方向文字为从质点 `M` 指向微元的向量 `(x,y,-a)`；最终引力向量保持负 `z` 方向。
- `#631` 补质点位于锥体顶点时的奇异核说明，采用 `\varepsilon\le r\le\sec\varphi` 截断后令 `\varepsilon\to0^+`。

## 3. 逐题内容复核摘要

| 全局号 | 复核结论 |
| ---: | --- |
| 613-615 | 直角、柱、球坐标化累次积分的投影、锥角、球面上界和 Jacobian 均已核对；`#615` 的球坐标表达完整。 |
| 616-619 | 常规三重积分、柱坐标计算、球坐标计算和偏心球/锥面综合题均复算通过，结果包括 `1/48`、`\frac12\ln2-\frac5{16}`、`672\pi`、`\frac{\pi}{6}(\sqrt2-1)` 等。 |
| 620 | 积分恒等式证明使用球体坐标轴置换对称性与固定 `z` 截面面积，逻辑闭合。 |
| 621-624 | 曲面围体积、平面质心、半球壳质心和变密度偏心球质心均按截面/对称性/静力矩标准公式复核通过。 |
| 625-626 | 平面薄片转动惯量和非均匀球体任一直径转动惯量公式正确，密度常数由总质量反解无误。 |
| 627-628 | `#627` 第 (1) 小题原错误已更正；第 (2) 小题 `6\pi` 与 `#628` 圆环质心 `(0,7/3)` 复核通过。 |
| 629-631 | 旋转体转动惯量、圆盘对轴上质点引力、顶点质点与圆锥体引力均按标准微元模型复核；方向、常量、反常积分条件已补齐。 |

本轮复核未发现本批仍残留硬性计算错误、区域边界错误或与前文方法口径冲突的问题。

## 4. 公式排版复核

按校准后的折算扫描源码范围 `27277-28142`：

- `inline_math_candidates_ge45 = 5`
- `hard_ge52_5 = 0`
- `near_45_52_5 = 5`

本轮曾发现 5 条硬超限候选，处理方式如下：

- `#627` 第 (2) 小题 `V_2` 长推导链改为行间公式。
- `#620` 三个三重积分相等式改为行间公式。
- `#629` 圆盘截面二重积分改为行间公式。
- `#619` 球坐标定义不抬成 display，而是拆成两个短行内片段。
- `#619` 椭球积分对称分解改成文字说明三个一阶矩，避免低层级长行内公式。

剩余 5 条 near 项均低于 `52.5`：两个恒等式题干/结论、一个绝对值积分建模式、一个旋转体惯量建模式、一个交线化简式。PDF 视觉检查未见挤压或越界，按用户口径保留行内。

## 5. PDF 版面复核

已重新渲染：

- `tmp/pdfs/batch_018_613_631/page-437.png` 至 `page-450.png`

检查结果：

- `page-438` 至 `page-440`：`#613-615` 的直角/柱/球坐标积分限清楚，`#615` 的完整球坐标代换和 `f(...)` 参数未压边。
- `page-441` 至 `page-444`：`#616-619` 的计算链、柱坐标/球坐标积分和分段绝对值积分排版可读，无公式重叠。
- `page-445` 至 `page-447`：`#620-624` 的对称性、截面法和质心静力矩显示正常。
- `page-448`：`#627` 第 (1) 小题更正后的 `2\ln2-\frac54` 推导清楚，未见拥挤。
- `page-449`：`#629-630` 的旋转体惯量和圆盘引力公式无溢出，方向文字已正确。
- `page-450`：`#631` 的截断反常积分与最终结果显示正常；下一节开头仅作边界观察。

## 6. 验证记录

- 重建索引：`python solution_quality_audit/build_solution_index.py`
  - `total_solutions = 1322`
  - `long_inline_flagged = 254`
- 编译：`latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex`
  - 结果：成功生成 `elegantbook2.pdf`
- 日志扫描：`Overfull|Underfull|LaTeX Warning|Package .* Warning|Undefined control sequence|Fatal|^!`
  - 结果：无输出
- PDF 页数：`721`
- `git diff --check -- elegantbook2.tex solution_quality_audit/solution_index.csv solution_quality_audit/summary.json`
  - 结果：仅有既有 Windows 换行提示，无空白错误

## 7. 放行结论

批次 `#613-631` 可放行进入下一批。放行依据包含：数学内容复算、推导步骤完整性、与本章三重积分/应用公式口径一致、硬长行内公式清零、PDF 页面视觉检查通过。
