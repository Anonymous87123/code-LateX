# 批次 019 整改复审记录

生成时间：2026-06-23

## 0. 执行边界

- 批次范围：全局 `#632-643`
- 源码范围：`elegantbook2.tex:28147-28499`
- PDF 复核页：物理页 `450-456`，对应印刷页 `400-406`
- 本批主题：重积分本章总习题，包括二重积分换序、对称性、极坐标、一般换元、质心、三重积分切片、柱坐标、球坐标与球壳积分。
- 本轮原则：按已确认审计报告修改当前批次；公式排版继续采用用户指定的 `3/4\textwidth` 口径，硬阈值为 `52.5/70` 折算单位。公式只是复核项之一，逐题同步核对解答正确性、推导完整性和与本章方法口径的一致性。

## 1. 已完成的主要正文整改

- `#634`：将题干表格中过长的积分等式拆成 `makecell` 内的短行，避免表格栏内拥挤。
- `#635`：补充 `x/(x^2+y^2)` 在原点未定义的处理口径，说明按去掉原点后取极限的反常积分理解，且极坐标面积元抵消 `1/r` 型奇性；长计算链改为行间展示。
- `#636`：去除正文中的参考答案勘误式长注，保留换序和分部积分推导；补出 `\int y\cos(\pi y/2)\,\mathrm{d}y` 的分部积分步骤。
- `#638`：补清一般换元后的区域、Jacobian 与累次积分计算链，删去重复压缩的长行内公式。
- `#640`：补充 `\sin x/x` 在 `x=0` 处按极限补值为 `1` 的说明；将末段积分计算链拆为清晰的行间公式。
- `#641`：去除正文中的参考答案勘误式文字，保留 `z=\sqrt3 r` 对应 `\varphi=\pi/6` 的几何推导。
- `#642`：题名由“利用球坐标系处理截断旋转体体积”改为“利用柱坐标系处理截断旋转体体积”，使题名、方法和积分限一致；柱坐标计算链改为行间公式。

## 2. 逐题内容复核摘要

| 全局号 | 复核结论 |
| ---: | --- |
| 632 | 四个填空均复算通过；换序题先还原区域再写新积分限，复杂边界题在 `x=0` 分段正确，体积题按圆域上方抛物面计算得 `8\pi`。 |
| 633 | 选择题答案 `(A),(C),(B),(D)` 复核通过；奇偶性、四卦限比例、积分平均值极限和绝对值分段均与前文方法一致。 |
| 634 | 全球、上半球、第一卦限球的对称性判断正确；三项为零的理由分别对应关于 `x=0` 或 `y=0` 的奇函数抵消。 |
| 635 | 极坐标边界 `0\leqslant\theta\leqslant\pi/4`、`0\leqslant r\leqslant2\sin\theta/\cos^2\theta` 正确；原点奇性说明完整，结果 `\ln2` 复算通过。 |
| 636 | 两段区域换序为 `1\leqslant y\leqslant2,\ y\leqslant x\leqslant y^2` 正确；内层积分和分部积分复算通过，结果为 `4(2+\pi)/\pi^3`。 |
| 637 | 第一象限单位四分之一圆域极坐标化正确；令 `u=r^2` 后结果 `\frac{\pi}{2}\ln2-\frac{\pi}{4}` 复算通过。 |
| 638 | 代换 `x=au^2,y=bv^2`、新区域 `u+v\leqslant1` 与 Jacobian `4abuv` 正确；结果 `ab^2/30` 复算通过。 |
| 639 | 配方得到圆心 `(1/2,1/2)`、面积 `3\pi/2`，再由形心公式得 `3\pi/2`，无硬算绕路。 |
| 640 | 三维区域切片顺序正确，`x=0` 处补值不影响积分；先积 `z`、再积 `y` 后得到 `\frac12\int_0^{\pi/2}(\pi/2-x)\sin x\,dx`，结果 `\pi/4-1/2` 复算通过。 |
| 641 | 投影圆 `x^2+(y-1/2)^2\leqslant1/4`、柱坐标 `r\leqslant\sin\theta`、球坐标 `\pi/6\leqslant\varphi\leqslant\pi/2` 均正确。 |
| 642 | 旋转后曲面为 `x^2+y^2=2z`，柱坐标范围 `0\leqslant r\leqslant4,\ r^2/2\leqslant z\leqslant8` 正确；结果 `1024\pi/3` 复算通过。 |
| 643 | 第一卦限球壳关于 `x` 与 `z` 对称，化为两倍 `z` 项合法；球坐标分离变量和径向积分复算通过，结果 `\pi(2e^3-5)/(4e^4)`。 |

本轮复核未发现本批仍残留的计算错误、区域边界错误、结论偏差或与本章重积分方法口径冲突的问题。

## 3. 公式排版复核

按用户口径，本批只处理两类排版问题：

- 折算宽度接近或超过 `52.5` 的硬超限行内公式，才转为 display 或改写为短行内片段。
- 短定义、小代入、小结论以及未达到硬阈值的 near 项，不机械拆为行间公式，结合 PDF 实际版面判断。

最终 PDF 目检中，以下位置作为 near 项人工观察后保留行内或原排版：

- `#632` 小题 (2) 换序结果式：PDF 中自然断在等号附近，无越界和挤压。
- `#632` 小题 (4) 体积结果链：实际版面可读，未出现强挤压。
- `#638` 末尾 Beta 型小积分：属于局部计算说明，保留行内。
- `#639` 配方与质心公式：公式虽中等长度，但未达到必须拆出的程度。
- `#640` 三重积分建模式：PDF 中未越界，且作为建模结果紧跟文字说明可读。
- `#641/#643` 分离变量中的短积分值：属于小代入和小结论，保留行内。

PDF 页 `450-456` 未见公式重叠、越界、表格挤压、标题方法冲突或行内公式硬挤问题。

## 4. PDF 版面复核

已重新渲染并检查：

- `tmp/pdfs/batch_019_632_643/page-450.png` 至 `page-456.png`

检查结果：

- `page-450`：`#632` 开始处表格和前批边界页正常；小题 (2) 换序结果未越界。
- `page-451`：`#632` 小题 (3)(4) 和 `#633` 选择题版面清楚，选项与解答无重叠。
- `page-452`：`#633` 后半与 `#634` 对称性题显示正常，行间公式未压边。
- `page-453`：`#635-637` 的原点奇性说明、换序积分和极坐标计算排版正常。
- `page-454`：`#638-640` 的 Jacobian、质心公式和 `\sin x/x` 补值说明清楚，无长行拥挤。
- `page-455`：`#641-643` 开始部分的柱/球坐标和题名修改显示正常，`#642` 已与柱坐标方法一致。
- `page-456`：`#643` 球坐标分离变量与最终结果显示正常，页面无残留版面问题。

## 5. 验证记录

- 重建索引：`python solution_quality_audit/build_solution_index.py`
  - `total_solutions = 1322`
  - `long_inline_flagged = 251`
- 编译：`latexmk -xelatex -interaction=nonstopmode -halt-on-error elegantbook2.tex`
  - 结果：`elegantbook2.pdf` 已是最新，目标文件正常
- 日志扫描：`Overfull|Underfull|LaTeX Warning|Package .* Warning|Undefined control sequence|Fatal|^!`
  - 结果：无输出
- PDF 页数：`722`
- `git diff --check -- elegantbook2.tex solution_quality_audit/solution_index.csv solution_quality_audit/solution_quality_ledger.md solution_quality_audit/summary.json`
  - 结果：仅有既有 Windows 换行提示，无空白错误

## 6. 放行结论

批次 `#632-643` 可放行进入下一批。放行依据包括：逐题数学复算通过、推导步骤与本章重积分方法口径一致、奇点和补值说明已补齐、题名方法冲突已修正、正文勘误口吻已移除、公式排版按 `3/4\textwidth` 标准人工复核通过、PDF 页面目检通过。
