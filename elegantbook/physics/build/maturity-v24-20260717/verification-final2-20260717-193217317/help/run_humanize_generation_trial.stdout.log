usage: run_humanize_generation_trial.py [-h] --output OUTPUT [--model MODEL]
                                        [--codex-bin CODEX_BIN]
                                        [--timeout TIMEOUT]
                                        [--format {json,text}]
                                        case_root

Run one sealed public humanize fixture through a fresh local Codex process.
The built-in local backend is capped at E2 because host-read isolation is
unverified.

positional arguments:
  case_root

options:
  -h, --help            show this help message and exit
  --output OUTPUT
  --model MODEL
  --codex-bin CODEX_BIN
  --timeout TIMEOUT
  --format {json,text}
