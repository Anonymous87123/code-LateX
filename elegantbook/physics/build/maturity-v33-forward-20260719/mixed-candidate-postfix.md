我不能承诺或针对 AIGC 检测率进行优化；下面仅按课程讲解的文风改写。

【题干】设函数 $f(x)=x^2-2x$，判断其在区间 $[0,2]$ 上的单调性。

先对函数求导，可得 $f'(x)=2x-2$。判断单调性，要看导数的符号：当 $x<1$ 时，$f'(x)<0$；当 $x>1$ 时，$f'(x)>0$。所以，函数先减后增。

这道题要掌握的是判断链条，不是只背结论：先对函数求导，再看导数符号；先划分区间，再写单调性。
