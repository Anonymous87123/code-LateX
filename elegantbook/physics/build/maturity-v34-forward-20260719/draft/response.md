问题二以 2022 年公报口径下的江豚 1249 头和长江鲟 438 尾作为初值与口径锚定。基线修复情景下，两者的归一化末值分别为 1.133 和 1.140。仅提高通道阻隔而不叠加污染时，末值分别降至 0.901 和 0.856，相对基线下降 20.5\% 和 24.9\%。将人工放流项置为 \(R_S=0\) 后，江豚末值为 1.145，相对基线变化为 +1.0\%；长江鲟末值为 0.519，相对基线下降 54.5\%。污染情景下，两者末值分别为 0.623 和 0.706，相对基线下降 45.0\% 和 38.0\%。

三个对照提供的是情景差异，而非独立验证。通道阻隔和栖息地碎片化的综合损失由 \(\delta_B\) 表示；将 \(R_S\) 置零的对照用于拆分自然恢复与人工放流补偿效应，江豚与长江鲟在这一对照中的变化并不相同；污染情景则用于比较污染叠加阻隔对基线收益的侵蚀。上述判断限于当前参数组与 2022 年公报归一化口径下的内部投影，不构成独立外部验证。\(M\) 和 \(V\) 仅表示传导和竞争状态，不是问题二的直接观测目标。

配置：`DRAFT / BALANCED / MODELING / CLEAN`；未提供作者样本，使用场景默认声线。

材料分类：`FACT_PAYLOAD=11`，`EDITORIAL_REQUIREMENT=5`，`FACT_BOUNDARY=0`。

状态：待审候选；`draft_surface_source_check=NOT_RUN`，`semantic_source_check=NOT_EVALUATED`。
