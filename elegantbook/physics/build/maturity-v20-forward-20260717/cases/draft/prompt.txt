请使用 humanize-academic-chinese Skill，以 facts.tex 为唯一 supplied artifact，按 DRAFT、BALANCED、MODELING、CLEAN 起草“问题二结果与讨论”。严格执行 facts.tex 末尾的 EDITORIAL_REQUIREMENT，不补造数值、参数、机制、来源、验证、引文或结论。只把两段 TeX 正文写入 output.tex；另将以 facts.tex 为第一个 artifact、output.tex 为第二个 artifact并使用 `--mode DRAFT --scene MODELING` 实际运行统一验证器得到的完整 JSON 写入 validation.json。验证结果是 PASS、REVIEW 还是 FAIL 都必须原样记录，不得修改稿件或 JSON 来追求某一状态。不要读取同目录之外的 v20 测试材料、历史输出、评测报告或预期答案。
