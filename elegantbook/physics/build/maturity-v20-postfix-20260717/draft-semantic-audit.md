# DRAFT 来源、语义与文风盲审

## 审计范围与结论

本次只对 `cases/draft/facts.tex`、`cases/draft/output.tex` 和 `cases/draft/validation.json` 进行盲审。未读取 prompt、Skill、forward 输出或历史评测。

**内容结论：通过，可在内容层面直接交付。** `output.tex` 的全部实质性陈述均由 `facts.tex` 明确蕴含；数值、单位、物种配对、情景条件、相对基线、对照用途以及内部投影/外部验证边界均正确。未发现事实错配、无来源数字、条件偷换、因果强化或越过证据边界的结论。唯一轻微遗漏是未单独给出“`R_S` 表示人工放流强度”的变量定义，但正文以“将人工放流项置为 `R_S=0`”准确表达了其操作含义，不影响本段理解和交付。

## 逐句事实核对

| 序号 | `output.tex` 句意 | 核对结果 | 审计说明 |
|---|---|---|---|
| 1 | 2022 年公报口径以江豚 1249 头、长江鲟 438 尾作为初值与口径锚定 | 明确蕴含 | 年份、物种、数值、量词和用途全部一致。 |
| 2 | 基线修复下两物种归一化末值为 1.133、1.140 | 明确蕴含 | 顺序保持为江豚、长江鲟，无配对错误。 |
| 3 | 仅提高通道阻隔且不叠加污染时，末值为 0.901、0.856，较基线下降 20.5\%、24.9\% | 明确蕴含 | 条件完整；数值、物种顺序和相对基线均一致。“降至”由下降百分比支持。 |
| 4 | `R_S=0` 后江豚末值 1.145，较基线变化 +1.0\% | 明确蕴含 | 数值和相对基线方向一致；“人工放流项”与事实中的变量含义相符。 |
| 5 | `R_S=0` 后长江鲟末值 0.519，较基线下降 54.5\% | 明确蕴含 | 数值、物种和基线关系全部一致。 |
| 6 | 污染情景下末值为 0.623、0.706，较基线下降 45.0\%、38.0\% | 明确蕴含 | 顺序仍为江豚、长江鲟；无数值或百分比错配。 |
| 7 | `delta_B` 表示通道阻隔和栖息地碎片化的综合损失 | 明确蕴含 | 变量状态定义完整保留。 |
| 8 | 仅提高通道阻隔时，两物种末值均低于基线 | 明确蕴含 | 由 0.901/0.856 及 20.5\%/24.9\% 的下降直接支持；属于保守概括。 |
| 9 | 污染情景用于比较污染叠加阻隔对基线收益的侵蚀 | 明确蕴含 | 原事实中的机制用途原样保留，未写成已经获得外部因果证明。 |
| 10 | 放流项置零后江豚未下降、长江鲟降幅较大，该对照用于拆分自然恢复与人工放流补偿效应 | 明确蕴含 | “未下降”由 +1.0\% 支持，“降幅较大”由 -54.5\% 支持；“用于拆分”准确保留用途边界。 |
| 11 | 认识限于当前参数组和 2022 公报归一化口径下的内部投影，不构成独立外部验证 | 明确蕴含 | 内部投影与外部验证边界完整、没有弱化。 |
| 12 | `M`、`V` 是传导和竞争状态，不是本问直接观测目标 | 明确蕴含 | 两变量的状态与非观测目标边界准确。 |

## 覆盖、强化与弱化

- **完整覆盖：** 19 个数字/单位表面项全部对应事实；四类情景（基线、仅阻隔、`R_S=0`、污染）均出现，且条件没有互换；两个机制用途和认识论边界均保留。
- **轻微遗漏：** 未另起一句明示“`R_S` 表示人工放流强度”。不过“将人工放流项置为 `R_S=0`”已建立变量与含义的准确对应，因此属于定义显式度不足，不是语义错误。
- **安全弱化：** 第二段用“未下降”概括 +1.0\%，用“降幅较大”概括 -54.5\%，用“均低于基线”概括两项精确降幅。精确值已在第一段给出，整体信息未丢失。
- **无不当强化：** 没有把情景投影写成实测结果，没有把“用于比较/拆分”写成“证明”，没有把内部投影冒充独立外部验证，也没有把 `M`、`V` 写成直接观测量。

## 文风与结构

- **自然度：8.6/10。** 语句通顺、术语稳定，数字密集但仍可读；“分别”“较基线”连续出现是结果段的合理重复。
- **作者感：8.1/10。** 有明确取舍，第二段从结果转入机制用途与边界，不只是机械搬运；但表述高度克制，个别句子仍接近事实摘要。
- **段落职责：9.4/10。** 第一段集中报告基线和三个对照，第二段解释变量、对照用途及认识论边界，严格符合两段式要求；无列表或小标题进入交付正文。
- **模板味：低（反向质量分 8.5/10）。** 未出现空泛开场、套话式总结或生硬的“首先/其次/最后”；结构规整但由任务本身决定，不构成明显模板腔。
- **综合文风：8.6/10。** 可作为“问题二结果与讨论”正文直接使用。

## 自动状态判断

`validation.json` 的硬约束、言语行为和文风信号层均为 `PASS`，表面来源检查也为 `PASS`；但 `semantic_source_check` 与 `academic_correctness` 仍是 `NOT_EVALUATED`，且 `semantic_entailment_certified=false`，唯一复核原因是 `semantic_source_not_evaluated`。因此，**现有自动状态应保持 `REVIEW`，退出码保持 `2`**。本人工盲审可以支持内容层面放行，但不应在没有把语义审计结果正式回写/认证到自动流程的情况下，把机器状态擅自改成 `PASS/0`。
