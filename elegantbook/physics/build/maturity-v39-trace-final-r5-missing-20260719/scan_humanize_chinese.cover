       #!/usr/bin/env python3
    1: """Locate reviewable Chinese academic-style signals without classifying authorship."""
       
    1: from __future__ import annotations
       
    1: import argparse
    1: import csv
    1: import io
    1: import json
    1: import re
    1: import sys
    1: from bisect import bisect_right
    1: from pathlib import Path
    1: from typing import Any, Iterable, Sequence
       
       
    1: DEFAULT_LEXICON = Path(__file__).resolve().parents[1] / "references" / "lexical-signals.json"
    1: DEFAULT_EXTENSIONS = {".md", ".markdown", ".tex", ".txt"}
    1: SENTENCE_BREAKS = "。！？!?；;\n"
    1: SCENE_CHOICES = ("ALL", "AUTO", "GENERAL", "COURSE", "MODELING", "RESEARCH")
       
       
    1: def load_lexicon(path: str | Path = DEFAULT_LEXICON) -> dict[str, Any]:
           """Load and minimally validate the lexical signal contract."""
   72:     lexicon_path = Path(path)
  144:     with lexicon_path.open("r", encoding="utf-8") as handle:
   72:         data = json.load(handle)
       
   72:     if not isinstance(data.get("signals"), list) or not data["signals"]:
>>>>>>         raise ValueError("lexicon must contain a non-empty signals list")
       
   72:     seen: set[str] = set()
   72:     required = {
               "id",
               "category",
               "label",
               "variants",
               "regex",
               "scenes",
               "severity",
               "threshold",
               "exclusions",
               "action",
               "rationale",
               "positive_examples",
               "negative_examples",
               "provenance",
           }
 2160:     for signal in data["signals"]:
 2088:         missing = required - signal.keys()
 2088:         if missing:
>>>>>>             raise ValueError(f"{signal.get('id', '<unknown>')} missing fields: {sorted(missing)}")
 2088:         signal_id = signal["id"]
 2088:         if signal_id in seen:
>>>>>>             raise ValueError(f"duplicate signal id: {signal_id}")
 2088:         seen.add(signal_id)
 2088:         if signal["action"] not in {"KEEP", "DELETE", "REWRITE", "REVIEW"}:
>>>>>>             raise ValueError(f"invalid action for {signal_id}: {signal['action']}")
 2088:         threshold = signal["threshold"]
 2088:         if threshold.get("window") not in {"document", "paragraph", "sentence", "line"}:
>>>>>>             raise ValueError(f"invalid threshold window for {signal_id}")
 2088:         if int(threshold.get("min_occurrences", 0)) < 1:
>>>>>>             raise ValueError(f"invalid min_occurrences for {signal_id}")
 5544:         for pattern in signal["regex"]:
 3456:             re.compile(pattern, re.IGNORECASE | re.MULTILINE)
 4104:         for exclusion in signal["exclusions"]:
 2016:             if set(exclusion) != {"scope", "regex", "reason"}:
>>>>>>                 raise ValueError(f"invalid exclusion contract for {signal_id}")
 2016:             re.compile(exclusion["regex"], re.IGNORECASE | re.MULTILINE)
   72:     return data
       
       
    1: def _markdown_fence_spans(text: str) -> list[tuple[int, int, str]]:
  214:     opening_re = re.compile(
  107:         r"(?m)^[ \t]{0,3}(?P<fence>`{3,}|~{3,})[^\n]*(?:\n|$)"
           )
  107:     spans: list[tuple[int, int, str]] = []
  107:     cursor = 0
  110:     while cursor < len(text):
  107:         opening = opening_re.search(text, cursor)
  107:         if opening is None:
  104:             break
    3:         fence = opening.group("fence")
    6:         closing_re = re.compile(
    3:             rf"(?m)^[ \t]{{0,3}}{re.escape(fence[0])}{{{len(fence)},}}[ \t]*(?:\n|$)"
               )
    3:         closing = closing_re.search(text, opening.end())
    3:         end = closing.end() if closing is not None else len(text)
    3:         spans.append((opening.start(), end, "markdown-fence"))
    3:         cursor = end
  107:     return spans
       
       
    4: def _raw_protected_spans(
    1:     text: str,
    2:     document_format: str = "markdown",
    1: ) -> list[tuple[int, int, str]]:
  107:     if document_format not in {"markdown", "tex"}:
>>>>>>         raise ValueError("document_format must be 'markdown' or 'tex'")
  107:     patterns = [
  107:         (
  107:             "latex-verbatim-or-math-environment",
  214:             re.compile(
  107:                 r"(?is)\\begin\{(equation\*?|align\*?|alignat\*?|alignedat|gather\*?|multline\*?|"
                       r"displaymath|math|verbatim\*?|lstlisting|minted)\}.*?\\end\{\1\}"
                   ),
               ),
  107:         (
  107:             "latex-exam-or-formal-statement-environment",
  214:             re.compile(
  107:                 r"(?is)\\begin\{(example\*?|exercise\*?|problem\*?|question\*?|"
                       r"theorem\*?|lemma\*?|proposition\*?|corollary\*?|definition\*?)\}"
                       r".*?\\end\{\1\}"
                   ),
               ),
  107:         ("display-math", re.compile(r"(?s)(?<!\\)\$\$.*?(?<!\\)\$\$|\\\[.*?\\\]")),
  107:         ("inline-math", re.compile(r"(?s)\\\(.*?\\\)|(?<!\\)\$(?!\$)(?:\\.|[^$\n])+?(?<!\\)\$")),
  107:         ("inline-code", re.compile(r"(?<!`)`[^`\n]+`(?!`)")),
  107:         ("markdown-quote", re.compile(r"(?m)^\s*>[^\n]*")),
  107:         ("chinese-double-quote", re.compile(r"“[^”\n]*”|「[^」\n]*」|『[^』\n]*』")),
  107:         ("chinese-single-quote", re.compile(r"‘[^’\n]*’")),
  107:         (
  107:             "latex-quote",
  214:             re.compile(
  107:                 r"``[^\n]*?''|\\(?:enquote|textquote)\{(?:[^{}\n]|\{[^{}\n]*\})*\}"
                   ),
               ),
  107:         ("ascii-quote", re.compile(r'(?<!\\)"[^"\n]*?(?<!\\)"')),
           ]
  107:     if document_format == "tex":
   75:         patterns.append(("latex-comment", re.compile(r"(?m)(?<!\\)%[^\n]*")))
  107:     spans = _markdown_fence_spans(text)
 1252:     for reason, pattern in patterns:
 2305:         spans.extend((match.start(), match.end(), reason) for match in pattern.finditer(text))
  107:     return spans
       
       
    1: def _merge_spans(spans: Iterable[tuple[int, int, str]]) -> list[tuple[int, int, str]]:
  107:     merged: list[list[Any]] = []
  143:     for start, end, reason in sorted(spans, key=lambda item: (item[0], item[1])):
   18:         if not merged or start >= merged[-1][1]:
   18:             merged.append([start, end, {reason}])
   18:             continue
>>>>>>         merged[-1][1] = max(merged[-1][1], end)
>>>>>>         merged[-1][2].add(reason)
  125:     return [(start, end, "+".join(sorted(reasons))) for start, end, reasons in merged]
       
       
    2: class ProtectedIndex:
    1:     """Find whether an offset falls inside code, math, comments, or quotations."""
       
    1:     def __init__(self, text: str, document_format: str = "markdown") -> None:
  107:         self.spans = _merge_spans(_raw_protected_spans(text, document_format))
  125:         self.starts = [span[0] for span in self.spans]
       
    1:     def reason_at(self, offset: int) -> str | None:
>>>>>>         index = bisect_right(self.starts, offset) - 1
>>>>>>         if index >= 0:
>>>>>>             start, end, reason = self.spans[index]
>>>>>>             if start <= offset < end:
>>>>>>                 return reason
>>>>>>         return None
       
    1:     def reason_for_span(self, start: int, end: int) -> str | None:
  129:         index = bisect_right(self.starts, start) - 1
  129:         if index >= 0:
   13:             protected_start, protected_end, reason = self.spans[index]
   13:             if protected_start <= start and end <= protected_end:
    7:                 return reason
  122:         return None
       
       
    1: def _signal_patterns(signal: dict[str, Any]) -> list[re.Pattern[str]]:
 2088:     patterns: list[re.Pattern[str]] = []
 2088:     if signal["variants"]:
 1512:         variants = sorted(set(signal["variants"]), key=len, reverse=True)
17496:         patterns.append(re.compile("|".join(re.escape(item) for item in variants), re.IGNORECASE))
 6264:     patterns.extend(
 3456:         re.compile(pattern, re.IGNORECASE | re.MULTILINE)
 7632:         for pattern in signal["regex"]
           )
 2088:     return patterns
       
       
    1: def _deduplicate_matches(matches: Iterable[re.Match[str]]) -> list[tuple[int, int, str]]:
 4176:     raw = sorted(
 4308:         ((match.start(), match.end(), match.group(0)) for match in matches),
 2220:         key=lambda item: (item[0], -(item[1] - item[0])),
           )
 2088:     kept: list[tuple[int, int, str]] = []
 2220:     for candidate in raw:
  132:         start, end, _ = candidate
  272:         if any(start < old_end and end > old_start for old_start, old_end, _ in kept):
    3:             continue
  129:         kept.append(candidate)
 2088:     return sorted(kept)
       
       
    1: def _bounded_span(text: str, offset: int, separators: str) -> tuple[int, int]:
  280:     left = max((text.rfind(char, 0, offset) for char in separators), default=-1) + 1
  252:     right_candidates = [text.find(char, offset) for char in separators]
  252:     right_candidates = [position for position in right_candidates if position >= 0]
   28:     right = min(right_candidates) + 1 if right_candidates else len(text)
   28:     return left, right
       
       
    1: def _paragraph_span(text: str, offset: int) -> tuple[int, int]:
   11:     separators = list(re.finditer(r"(?:\r?\n\s*){2,}", text))
   11:     start = 0
   11:     end = len(text)
   11:     for separator in separators:
>>>>>>         if separator.end() <= offset:
>>>>>>             start = separator.end()
>>>>>>         elif separator.start() >= offset:
>>>>>>             end = separator.start()
>>>>>>             break
   11:     return start, end
       
       
    1: def _scope_span(text: str, offset: int, scope: str) -> tuple[int, int]:
  150:     if scope == "document":
  115:         return 0, len(text)
   35:     if scope == "paragraph":
    7:         return _paragraph_span(text, offset)
   28:     if scope == "sentence":
   28:         return _bounded_span(text, offset, SENTENCE_BREAKS)
>>>>>>     if scope == "line":
>>>>>>         return _bounded_span(text, offset, "\n")
>>>>>>     raise ValueError(f"unsupported scope: {scope}")
       
       
    1: def _scope_bounds(text: str, start: int, end: int, scope: str) -> tuple[int, int]:
    7:     if scope == "match":
>>>>>>         return start, end
    7:     if scope == "context":
>>>>>>         return max(0, start - 100), min(len(text), end + 100)
    7:     return _scope_span(text, start, scope)
       
       
    6: def _exclusion_reason(
    1:     text: str,
    1:     start: int,
    1:     end: int,
    1:     signal: dict[str, Any],
    1: ) -> str | None:
  129:     for exclusion in signal["exclusions"]:
    7:         scope_start, scope_end = _scope_bounds(text, start, end, exclusion["scope"])
    7:         scoped = text[scope_start:scope_end]
    7:         for match in re.finditer(exclusion["regex"], scoped, re.IGNORECASE | re.MULTILINE):
>>>>>>             absolute_start = scope_start + match.start()
>>>>>>             absolute_end = scope_start + match.end()
>>>>>>             if absolute_start < end and absolute_end > start:
>>>>>>                 return exclusion["reason"]
  122:     return None
       
       
    1: def _line_column(text: str, offset: int) -> tuple[int, int]:
  126:     line = text.count("\n", 0, offset) + 1
  126:     last_break = text.rfind("\n", 0, offset)
  126:     column = offset - last_break
  126:     return line, column
       
       
    1: def _context(text: str, start: int, end: int, radius: int = 70) -> str:
  126:     snippet = text[max(0, start - radius):min(len(text), end + radius)]
  126:     return re.sub(r"\s+", " ", snippet).strip()
       
       
    1: def _window_key(text: str, offset: int, scope: str) -> tuple[int, int]:
  122:     return _scope_span(text, offset, scope)
       
       
    5: def _qualifying_counts(
    1:     text: str,
    1:     occurrences: Sequence[dict[str, Any]],
    1:     threshold: dict[str, Any],
    1: ) -> list[int]:
 2088:     scope = threshold["window"]
 2088:     max_chars = int(threshold.get("window_chars", 0) or 0)
 2088:     distinct_scope = threshold.get("count_distinct_by")
 2088:     if distinct_scope not in {None, "paragraph", "sentence", "line"}:
>>>>>>         raise ValueError(f"unsupported count_distinct_by: {distinct_scope}")
 2210:     keys = [_window_key(text, item["start"], scope) for item in occurrences]
 2088:     counts: list[int] = []
 2210:     for index, occurrence in enumerate(occurrences):
  122:         key = keys[index]
  244:         neighbors = [
  252:             item
  370:             for other_index, item in enumerate(occurrences)
  126:             if keys[other_index] == key
  126:             and (not max_chars or abs(item["start"] - occurrence["start"]) <= max_chars)
               ]
  122:         if distinct_scope:
>>>>>>             counts.append(
>>>>>>                 len({_window_key(text, item["start"], distinct_scope) for item in neighbors})
                   )
               else:
  122:             counts.append(len(neighbors))
 2088:     return counts
       
       
   12: def _finding(
           *,
    1:     text: str,
    1:     file: str,
    1:     scene: str,
    1:     signal: dict[str, Any],
    1:     occurrence: dict[str, Any],
    1:     count: int,
    1:     protected: bool,
    1:     protection: str | None,
    2:     excluded: bool = False,
    2:     exclusion: str | None = None,
    1: ) -> dict[str, Any]:
  126:     line, column = _line_column(text, occurrence["start"])
 2394:     return {
  126:         "start_char": occurrence["start"],
  126:         "end_char": occurrence["end"],
  126:         "file": file,
  126:         "line": line,
  126:         "column": column,
  126:         "matched": occurrence["matched"],
  126:         "context": _context(text, occurrence["start"], occurrence["end"]),
  126:         "count": count,
  126:         "action": "KEEP" if protected or excluded else signal["action"],
  126:         "signal_id": signal["id"],
  126:         "category": signal["category"],
  126:         "label": signal["label"],
  126:         "scene": scene,
  126:         "severity": signal["severity"],
  126:         "candidate": not protected and not excluded,
  126:         "protected": protected,
  126:         "protection": protection,
  126:         "excluded": excluded,
  126:         "exclusion": exclusion,
  126:         "rationale": signal["rationale"],
           }
       
       
    8: def scan_text_with_offsets(
    1:     text: str,
           *,
    2:     file: str = "<memory>",
    2:     scene: str = "ALL",
    2:     lexicon: dict[str, Any] | None = None,
    2:     include_protected: bool = False,
    2:     include_excluded: bool = False,
    1: ) -> list[dict[str, Any]]:
           """Return candidates with exact internal character offsets."""
   72:     scene = scene.upper()
   72:     if scene not in SCENE_CHOICES:
>>>>>>         raise ValueError(f"unsupported scene: {scene}")
   72:     lexicon = lexicon or load_lexicon()
   72:     suffix = Path(file).suffix.lower() if file and file != "<memory>" else ""
   72:     document_format = "tex" if suffix in {".tex", ".ltx"} else "markdown"
   72:     protected_index = ProtectedIndex(text, document_format=document_format)
   72:     findings: list[dict[str, Any]] = []
       
 2160:     for signal in lexicon["signals"]:
               if (
 2088:             scene not in {"ALL", "AUTO"}
>>>>>>             and "ALL" not in signal["scenes"]
>>>>>>             and scene not in signal["scenes"]
               ):
>>>>>>             continue
 2088:         raw_matches: list[re.Match[str]] = []
 7056:         for pattern in _signal_patterns(signal):
 4968:             raw_matches.extend(pattern.finditer(text))
 2088:         matches = _deduplicate_matches(raw_matches)
       
 2088:         candidates: list[dict[str, Any]] = []
 2088:         protected_hits: list[tuple[dict[str, Any], str]] = []
 2088:         excluded_hits: list[tuple[dict[str, Any], str]] = []
 2217:         for start, end, matched in matches:
  129:             occurrence = {"start": start, "end": end, "matched": matched}
  129:             protection = protected_index.reason_for_span(start, end)
  129:             if protection:
    7:                 protected_hits.append((occurrence, protection))
    7:                 continue
  122:             exclusion = _exclusion_reason(text, start, end, signal)
  122:             if exclusion:
>>>>>>                 excluded_hits.append((occurrence, exclusion))
>>>>>>                 continue
  122:             candidates.append(occurrence)
       
 2088:         counts = _qualifying_counts(text, candidates, signal["threshold"])
 2088:         minimum = int(signal["threshold"]["min_occurrences"])
 2210:         for occurrence, count in zip(candidates, counts):
  122:             if count >= minimum:
  238:                 findings.append(
  238:                     _finding(
  119:                         text=text,
  119:                         file=file,
  119:                         scene=scene,
  119:                         signal=signal,
  119:                         occurrence=occurrence,
  119:                         count=count,
  119:                         protected=False,
  119:                         protection=None,
                           )
                       )
 2088:         if include_protected:
 2095:             for occurrence, protection in protected_hits:
   14:                 findings.append(
   14:                     _finding(
    7:                         text=text,
    7:                         file=file,
    7:                         scene=scene,
    7:                         signal=signal,
    7:                         occurrence=occurrence,
    7:                         count=1,
    7:                         protected=True,
    7:                         protection=protection,
                           )
                       )
 2088:         if include_excluded:
 2088:             for occurrence, exclusion in excluded_hits:
>>>>>>                 findings.append(
>>>>>>                     _finding(
>>>>>>                         text=text,
>>>>>>                         file=file,
>>>>>>                         scene=scene,
>>>>>>                         signal=signal,
>>>>>>                         occurrence=occurrence,
>>>>>>                         count=1,
>>>>>>                         protected=False,
>>>>>>                         protection=None,
>>>>>>                         excluded=True,
>>>>>>                         exclusion=exclusion,
                           )
                       )
       
  198:     return sorted(findings, key=lambda item: (item["file"], item["line"], item["column"], item["signal_id"]))
       
       
    8: def scan_text(
    1:     text: str,
           *,
    2:     file: str = "<memory>",
    2:     scene: str = "ALL",
    2:     lexicon: dict[str, Any] | None = None,
    2:     include_protected: bool = False,
    2:     include_excluded: bool = False,
    1: ) -> list[dict[str, Any]]:
           """Return the stable public finding view without internal offsets."""
>>>>>>     findings = scan_text_with_offsets(
>>>>>>         text,
>>>>>>         file=file,
>>>>>>         scene=scene,
>>>>>>         lexicon=lexicon,
>>>>>>         include_protected=include_protected,
>>>>>>         include_excluded=include_excluded,
           )
>>>>>>     return [
>>>>>>         {key: value for key, value in finding.items() if key not in {"start_char", "end_char"}}
>>>>>>         for finding in findings
           ]
       
       
    7: def scan_file(
    1:     path: str | Path,
           *,
    2:     scene: str = "ALL",
    2:     lexicon: dict[str, Any] | None = None,
    2:     include_protected: bool = False,
    2:     include_excluded: bool = False,
    1: ) -> list[dict[str, Any]]:
>>>>>>     source = Path(path)
>>>>>>     text = source.read_text(encoding="utf-8-sig")
>>>>>>     return scan_text(
>>>>>>         text,
>>>>>>         file=str(source),
>>>>>>         scene=scene,
>>>>>>         lexicon=lexicon,
>>>>>>         include_protected=include_protected,
>>>>>>         include_excluded=include_excluded,
           )
       
       
    1: def collect_paths(paths: Sequence[str], extensions: set[str] | None = None) -> list[Path]:
>>>>>>     extensions = extensions or DEFAULT_EXTENSIONS
>>>>>>     collected: set[Path] = set()
>>>>>>     for raw in paths:
>>>>>>         path = Path(raw)
>>>>>>         if path.is_file():
>>>>>>             collected.add(path.resolve())
>>>>>>         elif path.is_dir():
>>>>>>             collected.update(
>>>>>>                 item.resolve()
>>>>>>                 for item in path.rglob("*")
>>>>>>                 if item.is_file() and item.suffix.lower() in extensions
                   )
               else:
>>>>>>             raise FileNotFoundError(raw)
>>>>>>     return sorted(collected, key=lambda item: str(item).lower())
       
       
    1: OUTPUT_FIELDS = (
           "file",
           "line",
           "column",
           "matched",
           "context",
           "count",
           "action",
           "signal_id",
           "category",
           "label",
           "scene",
           "severity",
           "candidate",
           "protected",
           "protection",
           "excluded",
           "exclusion",
           "rationale",
       )
       
       
    6: def render_output(
    1:     findings: Sequence[dict[str, Any]],
           *,
    1:     output_format: str,
    1:     notice: str,
    2:     coverage: dict[str, Any] | None = None,
    1: ) -> str:
>>>>>>     if output_format == "json":
>>>>>>         payload = {
>>>>>>             "notice": notice,
>>>>>>             "finding_count": len(findings),
>>>>>>             "candidate_count": sum(bool(item["candidate"]) for item in findings),
>>>>>>             "findings": list(findings),
>>>>>>             "coverage": coverage
>>>>>>             or {
>>>>>>                 "status": "NOT_REPORTED",
>>>>>>                 "requested_count": 0,
>>>>>>                 "scanned_count": 0,
>>>>>>                 "skipped_count": 0,
>>>>>>                 "requested_files": [],
>>>>>>                 "scanned_files": [],
>>>>>>                 "skipped_files": [],
                   },
               }
>>>>>>         return json.dumps(payload, ensure_ascii=False, indent=2) + "\n"
>>>>>>     if output_format == "csv":
>>>>>>         buffer = io.StringIO(newline="")
>>>>>>         writer = csv.DictWriter(buffer, fieldnames=OUTPUT_FIELDS, extrasaction="ignore")
>>>>>>         writer.writeheader()
>>>>>>         writer.writerows(findings)
>>>>>>         return buffer.getvalue()
>>>>>>     if output_format == "text":
>>>>>>         lines = [f"说明：{notice}"]
>>>>>>         for item in findings:
>>>>>>             state = "受保护" if item["protected"] else "已豁免" if item["excluded"] else "候选"
>>>>>>             lines.append(
>>>>>>                 f"{item['file']}:{item['line']}:{item['column']} "
>>>>>>                 f"[{item['signal_id']}/{item['severity']}/{item['action']}/{state}] "
>>>>>>                 f"{item['matched']} | count={item['count']} | {item['context']}"
                   )
>>>>>>         return "\n".join(lines) + "\n"
>>>>>>     raise ValueError(f"unsupported output format: {output_format}")
       
       
    1: def build_parser() -> argparse.ArgumentParser:
>>>>>>     parser = argparse.ArgumentParser(
>>>>>>         description="定位中文学术文本中的模板词和句壳候选；不判断作者身份。"
           )
>>>>>>     parser.add_argument("paths", nargs="+", help="UTF-8 文本文件或目录")
>>>>>>     parser.add_argument(
>>>>>>         "--scene",
>>>>>>         type=str.upper,
>>>>>>         choices=SCENE_CHOICES,
>>>>>>         default="ALL",
>>>>>>         help="大小写不敏感；AUTO 扫描全部场景信号，GENERAL 只扫描通用信号",
           )
>>>>>>     parser.add_argument("--format", choices=("json", "csv", "text"), default="text", dest="output_format")
>>>>>>     parser.add_argument("--output", help="输出文件；省略时写到标准输出")
>>>>>>     parser.add_argument("--lexicon", default=str(DEFAULT_LEXICON), help="lexical-signals.json 路径")
>>>>>>     parser.add_argument("--include-protected", action="store_true", help="显示代码、数学、注释和引文中的受保护命中")
>>>>>>     parser.add_argument("--include-excluded", action="store_true", help="显示因技术上下文而豁免的命中")
>>>>>>     return parser
       
       
    1: def main(argv: Sequence[str] | None = None) -> int:
>>>>>>     if hasattr(sys.stdout, "reconfigure"):
>>>>>>         sys.stdout.reconfigure(encoding="utf-8")
>>>>>>     if hasattr(sys.stderr, "reconfigure"):
>>>>>>         sys.stderr.reconfigure(encoding="utf-8")
>>>>>>     parser = build_parser()
>>>>>>     args = parser.parse_args(argv)
>>>>>>     try:
>>>>>>         lexicon = load_lexicon(args.lexicon)
>>>>>>         paths = collect_paths(args.paths)
>>>>>>     except (OSError, ValueError, json.JSONDecodeError) as error:
>>>>>>         parser.error(str(error))
       
>>>>>>     findings: list[dict[str, Any]] = []
>>>>>>     scanned: list[str] = []
>>>>>>     skipped: list[str] = []
>>>>>>     for path in paths:
>>>>>>         try:
>>>>>>             findings.extend(
>>>>>>                 scan_file(
>>>>>>                     path,
>>>>>>                     scene=args.scene,
>>>>>>                     lexicon=lexicon,
>>>>>>                     include_protected=args.include_protected,
>>>>>>                     include_excluded=args.include_excluded,
                       )
                   )
>>>>>>             scanned.append(str(path))
>>>>>>         except UnicodeDecodeError:
>>>>>>             skipped.append(str(path))
       
>>>>>>     coverage = {
>>>>>>         "status": "REVIEW" if skipped else "PASS",
>>>>>>         "requested_count": len(paths),
>>>>>>         "scanned_count": len(scanned),
>>>>>>         "skipped_count": len(skipped),
>>>>>>         "requested_files": [str(path) for path in paths],
>>>>>>         "scanned_files": scanned,
>>>>>>         "skipped_files": skipped,
           }
>>>>>>     rendered = render_output(
>>>>>>         findings,
>>>>>>         output_format=args.output_format,
>>>>>>         notice=lexicon["output_policy"],
>>>>>>         coverage=coverage,
           )
>>>>>>     if args.output:
>>>>>>         Path(args.output).write_text(rendered, encoding="utf-8", newline="")
           else:
>>>>>>         sys.stdout.write(rendered)
>>>>>>     for path in skipped:
>>>>>>         print(f"跳过非 UTF-8 或乱码文件：{path}", file=sys.stderr)
>>>>>>     return 2 if skipped else 0
       
       
    1: if __name__ == "__main__":
>>>>>>     raise SystemExit(main())
