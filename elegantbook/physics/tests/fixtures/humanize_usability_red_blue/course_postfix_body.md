【题干】设函数 $f(x)=x^2-2x$，判断其在区间 $[0,2]$ 上的单调性。

对函数求导，得 $f'(x)=2x-2$。

当 $x<1$ 时，$f'(x)<0$；当 $x>1$ 时，$f'(x)>0$。因此，函数先减后增，即在 $[0,1]$ 上单调递减，在 $[1,2]$ 上单调递增。

这里不是让你死记结论，判断时要按这条链条来做：先对函数求导，再看导数符号；先划分区间，再写单调性。
