温度响应模型设为 $R(T)=aT+b$，表中统一列出 20 ℃、25 ℃ 和 30 ℃ 三组数据，以保持实验口径一致。随后对参数 $a$ 在 $[0.8,1.2]$ 内进行扫描。结果表明，当 $a=1.06$ 时，验证集误差由 8.1% 降至 5.4%，但计算时间由 31 s 增至 44 s。

验证集误差的下降说明该参数设置改善了预测性能，同时也增加了计算开销。实验数据、参数扫描与验证集比较共同构成参数选择的依据，可为工程应用中的决策提供参考。模型仍可能在一定程度上受到传感器漂移影响，后续可将其拓展至更多应用场景。
