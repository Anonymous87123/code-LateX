我们构建温度响应模型 $R(T)=aT+b$，并将 20 ℃、25 ℃ 和 30 ℃ 三组数据按统一实验口径列入表中。参数 $a$ 的扫描范围为 $[0.8,1.2]$。参数扫描结果表明，当 $a=1.06$ 时，验证集误差由 8.1% 降至 5.4%，但计算时间由 31 s 增至 44 s。

该模型不仅全面提升了预测性能，也形成了从数据到决策的完整闭环，为工程应用提供有力支撑。模型或许仍可能会在一定程度上受到传感器漂移影响，因此后续工作可以进一步拓展应用场景。
