本文构建温度响应模型 $R(T)=aT+b$。实验数据在表中按统一口径列示，并保留 20 ℃、25 ℃ 和 30 ℃ 三组数据。对参数 $a$ 的扫描范围为 $[0.8,1.2]$。结果表明，当 $a=1.06$ 时，验证集误差由 8.1% 降至 5.4%，但计算时间由 31 s 增至 44 s。

该模型全面提升了预测性能，并形成从数据到决策的完整闭环，为工程应用提供有力支撑。不过，模型仍可能在一定程度上受到传感器漂移的影响，后续工作可进一步拓展应用场景。
